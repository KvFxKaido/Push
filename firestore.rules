rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Locked by default until app-specific collections and auth rules land.
      allow read, write: if false;
    }
  }
}
