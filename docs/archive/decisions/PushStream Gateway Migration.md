# PushStream Gateway Migration

Date: 2026-04-24 (last updated 2026-04-25)
Status: complete — every consumer (chat UI, agent roles, CLI daemon, CLI REPL, worker coder-job) iterates `PushStream<M>` events directly, every non-demo provider has a native `<provider>Stream` consuming `openAISSEPump`, and the legacy SSE machinery is fully retired. Both bridges (`providerStreamFnToPushStream` in 9a, `createProviderStreamAdapter` in 9b), the `ProviderStreamFn` callback shape, `legacyChatPushStream`, `PROVIDER_STREAM_CONFIGS`, the `streamSSEChat` / `streamSSEChatOnce` machinery, and the CLI's two queue/notify PushStream shims (`createDaemonProviderStream`, `buildPlannerPushStream`) are all deleted. CLI's `streamCompletion` is now a thin retry/timeout wrapper around the same `openAISSEPump` the web side uses.

Companion to: `Architecture Remediation Plan — Defusing the Big Four.md`

## Context

Push originally streamed model output through `ProviderStreamFn` — a 12-positional-arg callback signature in `lib/provider-contract.ts` that conflated transport (provider HTTP), runtime (agent role context, prompt assembly, sandbox awareness), and lifecycle (timers, abort, telemetry) responsibilities. The signature had grown organically as new agent capabilities landed; runtime concerns leaked into the transport seam every time a new param was needed (`workspaceContext`, `hasSandbox`, `systemPromptOverride`, `scratchpadContent`, `onPreCompact`, `todoContent`).

The **PushStream gateway** is a parallel, async-iterable streaming contract that separates the two concerns:

- **Gateway side (`PushStream`)** is a function `(req: PushStreamRequest) => AsyncIterable<PushStreamEvent>`. Pure transport: takes a request, yields a normalized event stream. Per-provider implementations live next to their HTTP/SDK glue.
- **Runtime side** consumes events. Reasoning normalization, tool-call parsing, and the legacy callback shape are layered on top via composable transducers and `createProviderStreamAdapter`.

The migration is incremental. The adapter exists as a bridge so agent roles can keep their callback consumption while providers move under the new contract. Once the consumer side is also migrated (agent roles directly iterating PushStream events), the adapter becomes deletable.

## Phased plan

The plan was sketched in the original design conversation that produced PR #365. Nine phases, in order:

1. **Land the contract types and the adapter.** Establish `PushStreamEvent`, `PushStreamRequest`, `PushStream`, and `createProviderStreamAdapter` in `lib/provider-contract.ts`. Adapter bridges PushStream → legacy `ProviderStreamFn` so existing consumers keep working unchanged.

2. **Port one provider end-to-end.** Validate the contract against a real provider before doubling down. Cloudflare Workers AI was chosen because its `env.AI.run` SDK binding meant the Worker (not the client) was the gateway consumer — exercises a different shape of the abstraction than the client-side path.

3. **Add the reasoning transducer.** Lift `<think>...</think>` splitting out of the legacy callback parser into `normalizeReasoning`, a `(stream) => stream` transducer that any PushStream can compose. Includes a per-stream latch so hybrid providers (emit native `reasoning_content` AND inline `<think>` tags in the same stream) don't double-report reasoning.

4. **Port a second provider with adapter-level safety nets.** OpenRouter — high-traffic, OpenAI-shaped SSE, more variety than Cloudflare. Required adding timer machinery (`eventTimeoutMs` / `contentTimeoutMs` / `totalTimeoutMs`) and runtime-context passthrough to the adapter so adapted-path providers wouldn't regress from the legacy `streamSSEChatOnce` safety net.

5. **Close the telemetry gap.** Adapter-level OpenTelemetry span coverage so adapted providers emit `model.stream` spans with the same attribute vocabulary as the legacy path. `lib/` stays OTEL-free via a hook-based `AdapterTelemetry` interface; the app-side caller wires the real tracer.

6. **Migrate one agent role off the 12-arg callback.** The runtime-side proof of the contract. Validates that a consumer can iterate `PushStream` events directly instead of going through the adapter — surfaces contract gaps that aren't visible from the provider side.

7. **Extract shared helpers.** Once two PushStream implementations exist, deduplicate the SSE pump (currently inlined in both `cloudflareStream` and `openrouterStream`).

8. **Port more OpenAI-compatible providers** (Zen, Kilocode, OpenAdapter, Nvidia, Blackbox).

9. **Retire `createProviderStreamAdapter`.** Terminal step — only after every agent role is on PushStream.

## What's shipped

| # | PR | Phase | Landed | Summary |
|---|---|---|---|---|
| 1 | [#365](https://github.com/KvFxKaido/Push/pull/365) | 1 | 2026-04-21 | Add `PushStream` types + `createProviderStreamAdapter` + 11 unit tests. Adapter bridges PushStream → legacy `ProviderStreamFn`. |
| 2 | [#366](https://github.com/KvFxKaido/Push/pull/366) | 2 | 2026-04-22 | First provider — Cloudflare Workers AI. `cloudflareStream: PushStream` consumes `env.AI.run`; `handleCloudflareChat` iterates the stream directly (Worker is the consumer). Bypasses the adapter because tuning params didn't flow through it; that revealed `tool_call_delta` would need to land later if hybrid models showed up. |
| 3 | [#369](https://github.com/KvFxKaido/Push/pull/369) | 3 | 2026-04-22 | `normalizeReasoning` transducer in `lib/reasoning-tokens.ts`. Splits inline `<think>...</think>` out of `text_delta` into `reasoning_delta` + `reasoning_end` events. Per-stream `nativeSeen` latch prevents double-reporting when a hybrid provider emits both channels. |
| 4 | [#384](https://github.com/KvFxKaido/Push/pull/384) | 4 | 2026-04-24 | Adapter timer machinery (`eventTimeoutMs` / `contentTimeoutMs` / `totalTimeoutMs` collapsed from legacy connect/idle/progress/stall/total to three reasons the adapter can actually observe), runtime-context passthrough on `PushStreamRequest`, OpenRouter port via `openrouterStream` + `createProviderStreamAdapter`. Required follow-up commits on adapter content-timer arming, `<think>` wiring through `normalizeReasoning`, and native `delta.tool_calls` accumulation/flush. |
| 5 | [#385](https://github.com/KvFxKaido/Push/pull/385) | 5 | 2026-04-24 | Adapter-level OpenTelemetry telemetry hook. `lib/` stays OTEL-free; the app exposes an `AdapterTelemetry` shape that the adapter calls with `wrap(ctx, run)` and the caller implements with `tracer.startActiveSpan`. Attribute names mirror the legacy `streamSSEChatOnce` span exactly so dashboards keyed on `push.provider`/`push.model`/`push.stream.chunk_count`/`push.usage.*` keep working. Includes single-Error-instance handling on timeout, `hasSandbox`/`workspaceMode` start-context fields, and a wrap-failure fallback so a broken tracer can't break the `ProviderStreamFn` contract. |
| 6 | [#389](https://github.com/KvFxKaido/Push/pull/389) | 6 | 2026-04-25 | First agent-role migration — Auditor. Replaces the `streamFn?: ProviderStreamFn` option with `stream?: PushStream<LlmMessage>` on both `runAuditor` and `runAuditorEvaluation`, iterates events via a new `iteratePushStreamText` helper in `lib/stream-utils.ts` (activity-reset idle timeout; `text_delta` accumulation; `reasoning_*` ignored). Adds the reverse bridge `providerStreamFnToPushStream` in `lib/provider-contract.ts` so providers without a native PushStream still work (legacy callbacks → events). App-side wrapper caches the bridged PushStream per underlying `streamFn` so the Auditor's coalescing key (`auditCoalesceKey`) keeps deduping concurrent identical audits. |
| 7 | [#390](https://github.com/KvFxKaido/Push/pull/390) | 8 (first port) | 2026-04-25 | First OpenAI-compatible-catalog port — OpenCode Zen. `zenStream: PushStream` in `app/src/lib/zen-stream.ts` mirrors `openrouterStream`'s shape (same SSE parsing, reasoning channel normalization, native `delta.tool_calls` accumulation); branch differences vs. OpenRouter are endpoint (`getZenGoMode()` switch between `ZEN_GO_URLS.chat` and `PROVIDER_URLS.zen.chat`) and the absence of OpenRouter-specific body fields (`session_id`, `trace`, `reasoning`). `streamZenChat` now composes `normalizeReasoning(zenStream(req))` through `createProviderStreamAdapter` with the same telemetry hook OpenRouter uses. 18 unit tests in `zen-stream.test.ts` parallel the OpenRouter coverage. PR #390 review also added a `tool_call_delta` event variant to `PushStreamEvent` (yielded per native `delta.tool_calls` fragment) so the adapter's `contentTimeoutMs` doesn't trip during long tool-arg buffering. |
| 8 | [#391](https://github.com/KvFxKaido/Push/pull/391) | 8 (second port) | 2026-04-25 | Second catalog port — Kilo Code. `kilocodeStream: PushStream` in `app/src/lib/kilocode-stream.ts` mirrors the Zen pattern with one fewer branch (no Go-mode endpoint switch — Kilo Code has a single `PROVIDER_URLS.kilocode.chat` endpoint). `streamKilocodeChat` now composes `normalizeReasoning(kilocodeStream(req))` through `createProviderStreamAdapter` with the same telemetry hook. 19 unit tests parallel the Zen coverage. The intentional duplication across three OpenAI-compatible streams (OpenRouter + Zen + Kilocode) is the on-ramp for Phase 7's shared SSE pump extraction. |
| 9 | [#392](https://github.com/KvFxKaido/Push/pull/392) | 7 | 2026-04-25 | Shared SSE pump extracted to `lib/openai-sse-pump.ts`. `openAISSEPump({ body, signal, isKnownToolName? })` owns the OpenAI-compatible SSE wire shape (`data:` framing, `[DONE]` sentinel, `choices[0].delta` parsing, native `delta.tool_calls` accumulation/flush, usage capture, `finish_reason` mapping, abort + reader release). The pure helpers (`mapOpenAIFinishReason`, `mapOpenAIUsage`, `stripTemplateTokens`) move with it. App-side knowledge — `KNOWN_TOOL_NAMES` — is injected via the `isKnownToolName` predicate rather than imported, so `lib/` stays free of app dependencies and the pump is callable from CLI. `openrouterStream` / `zenStream` / `kilocodeStream` shrink to: build body → fetch → error check → `yield* openAISSEPump({ body: response.body, signal, isKnownToolName })` (~95 lines down from ~280). Review surfaced two latent bugs that pre-dated Phase 7 and now live in one place: the abort race (`reader.cancel()` resolves the in-flight read with `{done:true}` rather than throwing, so the post-loop tail emitted a spurious `done` after an external abort — fixed by rechecking `signal.aborted` after the read) and the never-flushed `TextDecoder` (a final chunk ending mid-UTF-8 dropped its trailing character; a trailing complete frame without a closing newline went unparsed — fixed by lifting per-line parsing into `parseLine` and running the post-loop `decoder.decode()` flush + buffer through it before clean close). 37 unit tests total. |
| 10 | [#393](https://github.com/KvFxKaido/Push/pull/393) | 8 (final ports) | 2026-04-25 | Final OpenAI-compatible catalog ports — OpenAdapter, Nvidia NIM, Blackbox AI. Each is a ~85-line wrapper of body-build → fetch → error-check → `yield* openAISSEPump(...)` against the shared pump. `streamOpenAdapterChat` / `streamNvidiaChat` / `streamBlackboxChat` move from `streamProviderChat('<provider>', ...)` onto `createProviderStreamAdapter(normalizeReasoning(<provider>Stream), ...)` matching the OpenRouter / Zen / Kilo Code pattern. Blackbox's legacy `shouldResetStallOnReasoning: true` flag is covered by construction — the adapter's `contentTimeoutMs` already resets on `reasoning_delta`. 23 wrapper-focused unit tests across the three new streams (URL, Bearer auth, error prefix, body shape, max_tokens/temperature/top_p forwarding, native-tool-call flush via the predicate); the SSE parsing itself stays covered by the 37 pump tests rather than being re-asserted per provider. |
| 11 | [#394](https://github.com/KvFxKaido/Push/pull/394) | 6 (final agent sweep) | 2026-04-25 | Final agent-role sweep — Coder, Reviewer, Planner, Explorer, DeepReviewer all migrated off the 12-arg `ProviderStreamFn` callback. Each role now exposes a `stream?: PushStream<LlmMessage>` option and iterates events via `iteratePushStreamText` (the same helper Auditor introduced — kept as-is because every remaining target role consumes only text deltas; reasoning/tool callbacks were unused on these paths). Multi-round roles (Coder, Explorer, DeepReviewer) compose `callbacks.signal` with the helper's internal activity controller via `AbortSignal.any` so user cancellation still aborts the in-flight upstream call — preserving the legacy callback path's signal propagation. App-side wrappers cache the bridged PushStream by underlying `streamFn` identity in a per-shim `WeakMap<ProviderStreamFn, PushStream>` (mirrors the Auditor wrapper) so concurrent runs against the same provider see the same `PushStream` and the Reviewer's identity-keyed coalescing keeps deduping. CLI side: `cli/pushd.ts` wraps each `daemonStreamFn` with `providerStreamFnToPushStream` at the call site, and `cli/delegation-entry.ts` replaces the bespoke `PlannerStreamFn` adapter with a small native PushStream around `streamCompletion` (deletes the custom interface). Adds five lib-side test files paralleling the Auditor coverage shape (`planner-core.test.ts`, `reviewer-agent.test.ts`, `explorer-agent.test.ts`, `deep-reviewer-agent.test.ts`, `coder-agent.test.ts`) — each feeds the role a PushStream emitting a fixed event sequence and asserts the request envelope, text accumulation, and abort behaviour. Drive-by fix to the Reviewer's coalesce-cleanup chain: the post-run `.finally(...)` now ends in `.catch(() => {})` so an upstream-failure rejection from the cached `run` promise doesn't surface as an unhandled rejection (the original caller awaits `run` directly so the error itself is still observed). |

| 13 | _this branch_ | 9b (retire forward bridge) | 2026-04-25 | Retire `createProviderStreamAdapter` and the chat-UI seam that depended on it. Inlined the adapter's body — timer machinery (event/content/total), OTEL `model.stream` span shape, single-Error-on-timeout, abort composition, wrap-failure fallback — into a private `app/src/lib/iterate-chat-stream.ts` module that operates on `PushStream<ChatMessage>` directly. `streamChat` no longer dispatches through `getProviderStreamFn(provider).streamFn` → one of twelve `streamXChat` exports → `createProviderStreamAdapter(...)`; it now resolves the model/timeouts inline, applies `createChunkedEmitter` to `onToken` (preserving the per-word UI batching that `withChunkedEmitter` used to give the six adapter-routed providers), and feeds events directly into `iterateChatStream`. The chat UI's external callback API (`streamChat(messages, onToken, onDone, ...)`) is unchanged so `app/src/hooks/chat-send.ts` doesn't move. Deletes the twelve `streamXChat` exports (openrouter / zen / kilocode / openadapter / nvidia / blackbox + ollama / cloudflare / azure / bedrock / vertex), `streamProviderChat`, `getProviderStreamFn`, `withChunkedEmitter`, `StreamChatFn`, `buildAdapterTelemetry`, `createProviderStreamAdapter` (255 lines), `ProviderStreamFn`, and the four `Adapter*` interfaces (`AdapterTimeoutErrorMessages`, `AdapterTimeoutConfig`, `AdapterTelemetryStartContext`, `AdapterTelemetryEndResult`, `AdapterTelemetry`) from `lib/provider-contract.ts`. Drops `lib/provider-contract.test.ts` entirely (1142 lines — every test was for `createProviderStreamAdapter` and the deleted reverse bridge). Drops the three `withChunkedEmitter` test cases in `orchestrator-provider-routing.test.ts` and rewrites the two `getProviderStreamFn` routing tests to assert on `getProviderPushStream` identity (also lets us verify the per-provider memoization the 9a follow-up commit added). After 9b, `lib/provider-contract.ts` is down to 138 lines: the wire types (`LlmMessage`, `AIProviderType`, `StreamUsage`, `ChunkMetadata`, `PreCompactEvent`, `PushStreamEvent`, `PushStreamRequest`, `PushStream`) plus the `Review*` result types — no more bridge code, no more legacy callback shape. |
| 14 | [#397](https://github.com/KvFxKaido/Push/pull/397) | 9c (PROVIDER_STREAM_CONFIGS prune) | 2026-04-25 | Mechanical follow-up to 9b: prune the six dead adapter-routed entries from `PROVIDER_STREAM_CONFIGS` in `app/src/lib/orchestrator-provider-routing.ts` (openrouter / zen / kilocode / openadapter / nvidia / blackbox). Post-9b, only the five legacy SSE providers (ollama / cloudflare / azure / bedrock / vertex) reach `streamSSEChat` via `legacyChatPushStream`, which is the only remaining consumer of the registry. Drops the OpenRouter `bodyTransform` callback (its `reasoning` / `session_id` / `trace` fields now live directly in `openrouterStream`'s body builder) and the OpenRouter-specific imports that fed it (`openRouterModelSupportsReasoning`, `getReasoningEffort`, `getOpenRouterSessionId`, `buildOpenRouterTrace`) plus the Zen Go-mode imports (`ZEN_GO_URLS`, `getZenGoMode`) that only appeared in the deleted Zen entry. Audited `StreamProviderConfig` against the five surviving entries and dropped three fields with no remaining consumer: `bodyTransform` (only OpenRouter set it), `apiUrlOverride` (destructured by `streamSSEChatOnce` but no entry ever set it), and `errorMessages.keyMissing` (populated by `buildErrorMessages` and the Cloudflare hardcoded block but never read — `streamSSEChatOnce` doesn't reference it and `legacyChatPushStream` builds its own missing-key error from `providerType`). Updates the two test fixtures (`orchestrator-streaming.test.ts`, `orchestrator-timers.test.ts`) that constructed an `errorMessages.keyMissing` literal. Surviving `StreamProviderConfig` shape: identity (`name`, `apiUrl`, `apiKey`, `authHeader`, `model`, `providerType`), timer set (`connectTimeoutMs` / `idleTimeoutMs` / `progressTimeoutMs` / `stallTimeoutMs` / `totalTimeoutMs`), six-slot `errorMessages` (`connect` / `idle` / `progress` / `stall` / `total` / `network`), `parseError`, `checkFinishReason`, `shouldResetStallOnReasoning`, `extraHeaders` — every field still consumed by at least one of the five legacy entries. |
| 15 | [#398](https://github.com/KvFxKaido/Push/pull/398) | 10 (Ollama native port) | 2026-04-25 | First legacy-SSE port — Ollama Cloud. New `app/src/lib/ollama-stream.ts` (~85 lines: build messages → fetch `PROVIDER_URLS.ollama.chat` with Bearer auth → error-check → `yield* openAISSEPump(...)`) + 16-case wrapper coverage in `ollama-stream.test.ts` (15 in the initial commit + an empty-key Authorization-header omission case added in review) paralleling Zen / Kilo Code's wrapper-only test shape (URL, Bearer auth, error prefix, body shape, max_tokens/temperature/top_p forwarding, native-tool-call flush via predicate, both `reasoning` / `reasoning_content` field names, `<think>` composition with `normalizeReasoning`). The pump's SSE parsing stays covered by the 37 cases in `lib/openai-sse-pump.test.ts`. `getProviderPushStream('ollama')` now returns `normalizeReasoning(ollamaStream(req))` matching the OpenRouter / Zen / Kilo Code / OpenAdapter / Nvidia / Blackbox pattern; `'ollama'` joins `ADAPTER_ROUTED_PROVIDERS` so the chat-UI path applies the outer chunker + OTEL `model.stream` span via `iterateChatStream`. The legacy `ollama` entry is gone from `PROVIDER_STREAM_CONFIGS` and `legacyChatPushStream` no longer routes ollama; `getOllamaModelName` / `getOllamaKey` stay live for `resolveChatDefaultModel` and `PROVIDER_READY_CHECKS.ollama` respectively. **Semantic shift on the chat-UI path:** the legacy ollama config used `connectTimeoutMs: 30_000` / `idleTimeoutMs: 45_000` and a "server may be cold-starting" connect error hint; post-port `buildChatTimeouts` applies the standard `ADAPTER_ROUTED_PROVIDERS` timeouts (event 60s / content 60s / total 180s) and renders generic stream-stalled / response-exceeded messages — same UX shift the six adapter-routed providers absorbed in 9b. Idle is more lenient (45→60s) and the cold-start hint is gone; per-provider timeout overrides were declined to keep the chat-UI shape uniform across all seven adapter-routed providers. |
| 16 | [#399](https://github.com/KvFxKaido/Push/pull/399) | 10b (Cloudflare native port + auth-empty-key sweep) | 2026-04-25 | Two changes shipped together because the auth sweep touches every existing adapter-routed stream file and adding `cloudflare-stream.ts` reads as a natural same-PR addition. **Cloudflare native port** — new `app/src/lib/cloudflare-stream.ts` (~95 lines, plus 12 wrapper tests) modeled on `ollama-stream.ts`. Even simpler than Ollama: the Worker (`handleCloudflareChat` in `app/src/worker/worker-providers.ts`) already translates `env.AI.run` events into OpenAI-shape SSE before responding, so the client just needs the same `openAISSEPump` everyone else uses (the "structural outlier" caveat in the original doc was about the Worker side, not the client). No Bearer token — Cloudflare's Worker uses its `env.AI` binding for upstream auth, so the legacy config left `apiKey: ''` / `authHeader: null` and the new stream omits Authorization entirely. `getProviderPushStream('cloudflare')` now returns `normalizeReasoning(cloudflareStream(req))`; `'cloudflare'` joins `ADAPTER_ROUTED_PROVIDERS`; the legacy `cloudflare` entry is gone from `PROVIDER_STREAM_CONFIGS` and `legacyChatPushStream` no longer routes cloudflare. **Auth empty-key sweep** — applies the `apiKey ? { Authorization: 'Bearer ${apiKey}' } : {}` guard from #398's review fix to the other six adapter-routed streams (`openrouter` / `zen` / `kilocode` / `nvidia` / `blackbox` / `openadapter`), with one regression test per stream asserting Authorization is omitted when the client key is empty. Same correctness fix the Ollama review surfaced: `standardAuth` on the Worker reads any non-empty client `Authorization` as "key supplied" and skips the configured `keyMissingError` 401, so sending `Bearer ` (truthy) bypasses the fallback and forwards an empty bearer upstream; omitting the header lets the Worker (or upstream) surface a proper key-missing error. After 10b, all eight adapter-routed streams behave uniformly on missing keys. **Two semantic shifts on Cloudflare's chat-UI path** (documented as behaviour-path shifts, not UX regressions): (1) legacy `stallTimeoutMs: 90_000` (deliberately looser than standard, with a comment specifically calling out Kimi K2.6 reasoning preludes) becomes the standard 60s — but since the pump emits `reasoning_delta` for native `reasoning_content` content and the chat-UI timer treats those as activity, multi-minute thinking still keeps the timer alive; live verification on Kimi K2.6 / long reasoning preludes recommended before declaring this safe in production. (2) The "Cloudflare Workers AI is not configured on this Worker" preflight check moves from a client-side throw (legacy `buildConfig` checked `getCloudflareWorkerConfigured()` before any HTTP call) to a round-trip 401 from the Worker with the same message string — same user-facing text, different failure path. Per-provider timeout overrides in `buildChatTimeouts` were declined unless live verification proves the 60s standard breaks Cloudflare reasoning models. |
| 17 | [#400](https://github.com/KvFxKaido/Push/pull/400) | 10c (experimental three + `legacyChatPushStream` deletion) | 2026-04-25 | Terminal step on the provider side. Ports the last three legacy SSE providers — Azure OpenAI, AWS Bedrock, Google Vertex — onto native `<provider>Stream` modules consuming `openAISSEPump`, then deletes the surviving legacy machinery (`legacyChatPushStream`, `PROVIDER_STREAM_CONFIGS`, `ProviderStreamEntry`, `buildExperimentalStreamConfig`, `buildVertexStreamConfig`, `buildErrorMessages`, the legacy 5-key `STANDARD_TIMEOUTS` shape). After 10c, every non-demo provider is adapter-routed, `getProviderPushStream` is a single switch over native streams (no fallthrough to a legacy path), and `streamSSEChat` / `streamSSEChatOnce` in `app/src/lib/orchestrator.ts` have no remaining production callers — left in place for now (still tested by `orchestrator-timers.test.ts` directly) but flagged as a follow-up cleanup. **Per-provider notes:** Azure / Bedrock are structural twins — both fetch `PROVIDER_URLS.<provider>.chat`, send `X-Push-Upstream-Base` (built via `buildExperimentalProxyHeaders`) plus the empty-key Authorization guard, and inherit the same baseURL-validation throw the legacy `buildExperimentalStreamConfig` did (`normalizeExperimentalBaseUrl` rejects non-`<resource>.openai.azure.com/openai/v1` and non-`bedrock-runtime.<region>.amazonaws.com/openai/v1` URLs respectively). Vertex is dual-mode: in `native` mode the stream encodes the configured service account into `X-Push-Vertex-Service-Account` (via `encodeVertexServiceAccountHeader`) plus a normalized `X-Push-Vertex-Region` and sends no Authorization — the Worker mints the Google access token from the encoded service account; in `legacy` mode the stream falls back to the Azure / Bedrock shape (Bearer + `X-Push-Upstream-Base`). Vertex Anthropic-transport models (`claude-*` IDs) keep working because the Worker's existing `createAnthropicTranslatedStream` translates the Anthropic-shape upstream response to OpenAI-shape SSE before forwarding — invisible to the client and to `openAISSEPump`. Test coverage: 11 tests for Azure, 11 for Bedrock, 14 for Vertex (the extras cover the legacy↔native mode switch, the missing-service-account / invalid-JSON / malformed-region throws, and the headers-not-mixed-across-modes assertions). All eleven providers now route through the same gateway pattern and `lib/openai-sse-pump.ts`'s 37 cases continue to cover the actual SSE parsing. **`ADAPTER_ROUTED_PROVIDERS`** now lists every real provider — the `useOuterWrap` check in `streamChat` reduces to "anything but demo", documented inline. **Behaviour shifts:** The chat-UI timer for the experimental three is uniform (event 60s / content 60s / total 180s) instead of the legacy 5-timer scheme; this matches the Phase 10 / 10b shift the other adapter-routed providers absorbed and brings the surviving `STANDARD_TIMEOUTS` const down to the 3 fields `buildChatTimeouts` actually consumes. |
| 18 | _this branch_ | 10d (orchestrator SSE dead-code cleanup) | 2026-04-25 | Closes out the migration. After 10c, `streamSSEChat` / `streamSSEChatOnce` / `createThinkTokenParser` (~430 lines combined) in `app/src/lib/orchestrator.ts` and `selectTimeoutMessage` / `TimeoutAbortReason` / `TimeoutDurations` / `hasFinishReason` / `StreamProviderConfig` (~80 lines) in `app/src/lib/orchestrator-streaming.ts` had no remaining production callers — the only consumers were two test files (`orchestrator-timers.test.ts`, `orchestrator-streaming.test.ts`) that were always self-referential, asserting on the timer / abort / span machinery rather than any specific provider. Both test files delete (~390 lines combined). `orchestrator-streaming.ts` shrinks from 140 lines to 14 — just `parseProviderError` (still consumed by all 11 native streams for upstream-error parsing) and the `ChunkMetadata` / `StreamUsage` re-exports. `orchestrator.ts` shrinks from 1061 lines to ~466. Drops the now-dead imports (`KNOWN_TOOL_NAMES`, `REQUEST_ID_HEADER`, `createRequestId`, the entire `tracing` import block — `getPushTracer`, `injectTraceHeaders`, `recordSpanError`, `setSpanAttributes`, `SpanKind`, `SpanStatusCode` — all only used inside `streamSSEChatOnce`) and the now-stale `toLLMMessages` doc comment that pointed to the deleted SSE path. Net diff: ~−900 lines vs. ~+10. The migration goal of "every consumer iterates `PushStream<M>`" is already met by Phase 9; this PR just removes the dead carcass left by 10c. |
| 19 | [#402](https://github.com/KvFxKaido/Push/pull/402) | 11 (CLI gateway adoption) | 2026-04-25 | Ports `cli/provider.ts#streamCompletion` onto the shared PushStream gateway. New `cli/openai-stream.ts` is a native `PushStream<LlmMessage>` mirroring `app/src/lib/openrouter-stream.ts`'s shape: build per-provider URL/headers/body from `PROVIDER_CONFIGS`, fetch, then `yield* openAISSEPump(...)`. One generic stream parameterized by `ProviderConfig` covers every CLI provider (ollama / openrouter / zen / nvidia / kilocode / blackbox / openadapter — all OpenAI-compatible chat/completions); OpenRouter-specific `HTTP-Referer` / `X-Title` headers and `session_id` / `trace` body fields branch on `config.id`. `streamCompletion` keeps its callback signature and 3-attempt retry policy (429 / 5xx / network) but is now a thin wrapper that iterates `normalizeReasoning(stream(...))` and drives `onToken` / `onThinkingToken` from `text_delta` / `reasoning_delta` / `reasoning_end` events; retries are gated on a `yieldedAny` flag so a mid-stream failure can't replay events on retry. Drops ~140 lines of inlined SSE parsing and the local `createReasoningTokenParser` from `cli/provider.ts` (re-exported from `lib/reasoning-tokens.ts` to keep the test imports alive). PR review surfaced and fixed five issues in a follow-up commit (`68b8046`): a P1 abort-after-stream-exit (the pump exits cleanly on `signal.aborted` so a mid-stream cancel could leak as a truncated success — added a post-loop signal check + `postLoopAbort` sentinel so the catch arm doesn't retry an already-translated abort), `keepalive: true` removal (browser-only flag with a 64KiB body cap that long chat histories would routinely exceed), a dead `(m.role as LlmMessage['role']) \|\| 'user'` fallback (the cast made the `\|\|` unreachable for non-empty strings — replaced with explicit allow-list narrowing), and a stale `CliProviderError` docstring. CLI test suite: 1327/1327 pass. |
| 20 | [#403](https://github.com/KvFxKaido/Push/pull/403) | 11b (CLI shim cleanup) | 2026-04-25 | Drops the two queue/notify PushStream shims that bridged `streamCompletion`'s callback shape into a `PushStream` for lib-side agent roles. `cli/daemon-provider-stream.ts` shrinks from 140 lines to 45: the factory now calls `createCliProviderStream` and pipes through `normalizeReasoning`, with `resolveApiKey` deferred to first iteration. The synthetic `done: aborted` event handling is retired — `openAISSEPump` returns cleanly on `signal.aborted` and the agent-role consumers (`iteratePushStreamText`) compose their own cancellation signals at the call site. `cli/delegation-entry.ts#buildPlannerPushStream` shrinks from 90 lines to 15: the outer SIGINT controller now composes with `req.signal` via `AbortSignal.any` at the wrapper boundary, mirroring how `lib/coder-agent.ts` and friends compose user-cancel with the activity-reset timeout. **Behaviour shift:** the daemon and planner paths no longer inherit `streamCompletion`'s 3-attempt retry policy on 429/5xx/network — they surface transient failures through the iterator the same way `app/src/lib/openrouter-stream.ts` and the rest of the web side do. `cli/engine.ts`'s legacy REPL/headless path keeps `streamCompletion` unchanged, so its retries stay; the asymmetry is intentional (chat REPL benefits from auto-retry, lib agent-role consumers compose their own error handling). `runPlannerCore` is fail-open, so a transient planner failure degrades to "run the task without delegation" rather than three silent retries. Test pruning: drops the `createReasoningTokenParser` describe block (~25 tests) and the lone `reasoning token routing` test inside `streamCompletion` — both fully covered by `lib/reasoning-tokens.test.ts`'s `normalizeReasoning` suite. PR review surfaced two issues in a follow-up commit (`4fc18aa`): added inline doc comment on `buildPlannerPushStream` explicitly naming the planner retry-loss + fail-open user-visible effect (the original commit message only documented the daemon path), and re-added a focused `reasoning token routing (integration)` describe with two cases that exercise the seam `cli/engine.ts` depends on for `assistant_thinking_*` events. Net diff across the four files: −470 / +35 lines. CLI test suite: 1306/1306 pass. |

Provider side now fully ported:
- All eleven providers shipped via the gateway: Ollama Cloud + Cloudflare Workers AI + OpenRouter + OpenCode Zen + Kilo Code + OpenAdapter + Nvidia NIM + Blackbox AI + Azure OpenAI + AWS Bedrock + Google Vertex (including Vertex's dual native / legacy mode + Anthropic-transport via Worker translation).
- Reasoning channel normalization tested across both `<think>` tags and native `reasoning_content` / `reasoning` field renames.
- Timer machinery has parity with the legacy `streamSSEChatOnce` safety net.
- Telemetry attribute vocabulary preserved.
- Adapter handles upstream throws, timeouts, and external aborts cleanly.
- OpenAI-compatible SSE parsing consolidated into a single `lib/`-side pump shared across all eleven providers; each provider is now ~85–110 lines of fetch + error-mapping + delegate.
- All eleven streams omit Authorization when no client key is configured (Vertex native mode never sends it at all), so the Worker's `keyMissingError` / `runPreamble` 401 fires reliably instead of being bypassed by an empty `Bearer ` header.
- `legacyChatPushStream` is gone — there is no remaining bridge between `streamSSEChat` and the PushStream gateway.

Consumer side now also fully migrated:
- Every agent role (Auditor, Coder, Reviewer, Planner, Explorer, DeepReviewer) iterates `PushStream` events directly via `iteratePushStreamText`.
- `iteratePushStreamText` stayed unchanged through the sweep — every remaining target role consumed only text deltas, so growing it with optional reasoning/tool handlers would have been speculative API. The activity-reset timer already counts `reasoning_delta` / `tool_call_delta` events as keep-alive.
- Multi-round roles compose `callbacks.signal` with the helper's internal abort controller via `AbortSignal.any` so user-initiated cancellation still aborts the in-flight upstream call.
- App-side wrappers cache bridged PushStreams by underlying `ProviderStreamFn` identity in a per-shim `WeakMap`, preserving the Reviewer's identity-keyed coalescing.
- CLI's `cli/delegation-entry.ts` replaced its bespoke `PlannerStreamFn` adapter shape with a small native PushStream around `streamCompletion`; the custom interface is gone.

## What's left

Nothing. The CLI tranche (#402, #403) closed the last open seam: `cli/provider.ts#streamCompletion` is now a thin wrapper around `openAISSEPump`, and the two queue/notify PushStream shims that bridged the callback API for lib-side agent roles (`createDaemonProviderStream`, `buildPlannerPushStream`) are gone in favour of the native PushStream. Both `cli/engine.ts`'s legacy REPL/headless path and the daemon/planner paths share the same gateway, parser, and reasoning normalizer with the web side.

The one minor follow-up that doesn't gate completion: `lib/reasoning-tokens.ts#createReasoningTokenParser` (the callback-shaped legacy parser) now has zero production consumers across web and CLI. It's still exported from `lib/` but only because removing it would also need to decide between deleting the (~25-case) CLI test block currently driving it through the CLI re-export and porting those cases over to a `lib/`-side test file. Tracked as a decoupled cleanup PR.

## Recommendation for next

**Migration is fully complete.** Every consumer (chat UI, agent roles, CLI daemon, CLI REPL, worker coder-job) iterates `PushStream<M>` events directly. Every non-demo provider routes through a native `<provider>Stream` consuming `openAISSEPump` — the same parser shared between web and CLI. Both bridges, the `ProviderStreamFn` callback shape, the legacy SSE machinery (`streamSSEChat` / `streamSSEChatOnce` / `selectTimeoutMessage` / `StreamProviderConfig` / `legacyChatPushStream` / `PROVIDER_STREAM_CONFIGS`), and the CLI's two queue/notify PushStream shims (`createDaemonProviderStream`, `buildPlannerPushStream`) are all deleted. `getProviderPushStream` (web) and `createCliProviderStream` (CLI) are both single switches over native streams.

## Decisions captured along the way

- **Event union over message union.** `PushStreamEvent` is a discriminated union of small variants (`text_delta`, `reasoning_delta`, `reasoning_end`, `done`) rather than a richer "message" type. Lets transducers compose without reaching into payload internals.

- **`reasoning_end` as a distinct event, not a flag.** Adding a structural variant kept content vs. structural progress distinguishable in the timer machinery (Phase 4 fix #1) without complicating the `text_delta` / `reasoning_delta` shapes.

- **Adapter timer model collapses 5 → 3.** Legacy `streamSSEChatOnce` has connect/idle/progress/stall/total. The adapter sees parsed events, not raw bytes — connect/idle/progress all manifest as "no event arrived" and were merged into a single `eventTimeoutMs`. `contentTimeoutMs` maps to stall; `totalTimeoutMs` is wall clock. Recorded in #384.

- **`lib/` stays OpenTelemetry-free.** The telemetry hook (`AdapterTelemetry`) is dependency-free in the contract; the app-side caller wires the actual tracer. Same pattern preserves `lib/`'s ability to run in CLI without pulling OTEL.

- **Per-stream native-reasoning latch in `normalizeReasoning`.** Once a stream emits any native `reasoning_delta`, subsequent `text_delta` events bypass `<think>` parsing for the rest of that stream. Forward-compatible with hybrid providers; landed in #369.

- **Cloudflare Worker bypasses the adapter.** `handleCloudflareChat` iterates `cloudflareStream` directly, while OpenRouter's `streamOpenRouterChat` builds the adapter. Reason: Cloudflare's Worker is the gateway consumer (uses `env.AI.run`), while OpenRouter goes through `/api/openrouter/chat` proxy and the *client* is the consumer. Different shapes; the adapter is for the client-side legacy-callback case.

- **Single Error instance on timeout (Phase 5 fix #2).** Telemetry's `terminalError` and the caller's `onError` receive the same Error object. Matters for stack-trace inspection and identity checks downstream.

- **Wrap-failure fallback (Phase 5 fix #5).** If `telemetry.wrap` rejects before invoking `run`, the adapter falls back to running without telemetry so a broken tracer can't break the `ProviderStreamFn` contract. `runEntered` sentinel distinguishes pre-run from post-run rejections; post-run rejections are swallowed (stream already settled).

- **Reverse bridge in Phase 6 (`providerStreamFnToPushStream`).** During the staged migration, agent roles that consume PushStream events still need to talk to providers that haven't been ported off `ProviderStreamFn` yet. Rather than dual-wiring each consumer (native PushStream vs. legacy callback path), the Auditor migration added a single bridge in `lib/provider-contract.ts` that wraps any `ProviderStreamFn` as a `PushStream`. The bridge is the symmetric inverse of `createProviderStreamAdapter` — once Phase 8 finishes and every provider has a native PushStream, both bridges become deletable together with Phase 9.

- **`tool_call_delta` as a content-progress signal (Phase 8 follow-up to PR #390 review).** Native `delta.tool_calls` accumulation buffers fragments inside the provider stream and only flushes as a fenced-JSON `text_delta` on `finish_reason` / `[DONE]`. While buffering, the adapter saw no events and could trip `contentTimeoutMs` during long tool-arg payloads — a regression vs. the legacy `streamSSEChatOnce` path which treated tool-call frames as activity. Added `{ type: 'tool_call_delta' }` as a structural variant the adapter treats as content progress (resets `contentTimeoutMs`); both `openrouterStream` and `zenStream` yield one per fragment. The deferred-fix posture in the original Open questions was inverted because the Phase 8 codex review surfaced the regression preemptively rather than waiting for it to bite in practice.

- **PushStream caching by streamFn identity (Auditor wrapper).** The app-side `resolveAuditorPushStream` caches the bridged PushStream in a `WeakMap` keyed by the underlying `streamFn` so successive `runAuditor` calls for the same provider see the same `PushStream` object. The `auditCoalesceKey` uses stream identity to dedupe concurrent identical audits; without the cache, a fresh wrapper per call would defeat coalescing.

- **Shared SSE pump lives in `lib/`, app knowledge is injected (Phase 7).** `openAISSEPump` lives at `lib/openai-sse-pump.ts` rather than app-side. The pump is pure SSE-shape parsing and has no app-side imports: the only app-/runtime-specific input — the known-tool-names registry — flows in via an `isKnownToolName?: (name: string) => boolean` predicate. Choosing `lib/` over app-side closes the long-standing "Should the SSE pump live in `lib/` or app-side?" open question on the side of CLI-readiness: when `cli/provider.ts` migrates to PushStream, the pump is already callable without a second extraction. The cost was one extra config field on each adapter call site. Provider adapters keep ownership of fetch, endpoint URL, auth, body construction, provider-specific body extensions (OpenRouter's `reasoning` / `session_id` / `trace`, Zen's Go-mode URL switch), prompt assembly via `toLLMMessages`, and error mapping — the pump only sees a parsed response body and yields events.

- **Phase 8 final ports test the wrapper, not the parser.** The OpenAdapter / Nvidia / Blackbox tests (PR #393) carry ~8 cases each focused on the wrapper-specific surface (URL, Bearer auth, error prefix, body shape, max_tokens/temperature/top_p forwarding, native-tool-call flush via the predicate) rather than copying the OpenRouter / Zen / Kilocode coverage in full. Repeating the SSE parsing assertions per provider would have been retroactive duplication — the 37 `lib/openai-sse-pump.test.ts` cases already cover those corners once. The earlier per-provider tests stay in place because changing them is out of scope for the port; new ports lean on the shared pump tests as the source of truth for parsing behaviour.

- **Phase 6 final sweep keeps `iteratePushStreamText` unchanged.** Every remaining migration target (Coder, Reviewer, Planner, Explorer, DeepReviewer) consumed only text deltas — `onThinkingToken` was uniformly passed as `undefined` and `onPreCompact` was never wired. Growing the helper with optional reasoning/tool handlers during the sweep would have been speculative API with zero consumers; the activity-reset timer already counts `reasoning_delta` and `tool_call_delta` events as keep-alive without exposing them to the caller. If a future role wants to surface reasoning to the UI (status-bar reasoning snippets currently come from parsing the accumulated text rather than from native reasoning channels), add an optional handler then with a real consumer to anchor the shape.

- **Multi-round roles compose `callbacks.signal` via `AbortSignal.any` (Phase 6 final sweep).** The legacy callback path forwarded `callbacks.signal` as the 11th positional arg into `streamFn` so user cancellation aborted the in-flight upstream call. `iteratePushStreamText` only owns its own activity-timeout controller, so Coder / Explorer / DeepReviewer (and the Coder's `generateCheckpointAnswer` checkpoint helper) compose their `callbacks.signal` with the helper's internal `req.signal` via `AbortSignal.any([req.signal, externalSignal])` at a thin wrapper around the underlying stream. Auditor / Reviewer / Planner don't need this composition because they don't expose a callbacks-level abort signal in the first place — their only cancellation path is the activity timer.

- **CLI's bespoke `PlannerStreamFn` adapter shape is deleted (Phase 6 final sweep).** Pre-sweep, `lib/planner-core.ts` exported a custom `PlannerStreamFn` interface that web (`app/src/lib/planner-agent.ts`) and CLI (`cli/delegation-entry.ts`) both adapted into. The web adapter wrapped a `ProviderStreamFn`; the CLI adapter wrapped its own `streamCompletion`. Both are now gone. Web's planner-agent uses `providerStreamFnToPushStream` like every other role's web shim. CLI's `delegation-entry` builds a small native `PushStream` that wraps `streamCompletion` directly with a queue/wake async generator (composing the SIGINT signal and the consumer's per-stream signal). Net effect: planner-core's surface drops one type and one indirection layer; the migration is symmetric across surfaces.

## Open questions

- **`PushStreamEvent.done.finishReason` enum coverage.** Currently `'stop' | 'length' | 'tool_calls' | 'aborted' | 'unknown'`. Some providers emit `'content_filter'`, `'function_call'`, `'end_turn'`. Not currently a problem because `openrouterStream` and `cloudflareStream` map their own native reasons into the union. Will become a problem if more providers want richer reasons.

- **CLI consumption.** The CLI has its own provider streaming code today (`cli/provider.ts`). PushStream and now the shared `openAISSEPump` both live in `lib/` precisely so CLI can adopt them, but no CLI work has been done yet. Probably waits until at least one more agent role (Coder) migrates to PushStream consumption so we know the runtime-side shape is settled.

## Out of scope (deliberately)

- Native function-calling tool support beyond the text-fenced JSON convention Push uses today.
- Multi-modal input handling on `LlmMessage` (currently text-only; `ChatMessage` handles attachments at the runtime layer).
- Replacing the `ProviderStreamFn` legacy shape outright. It stays until every consumer migrates.
- CLI-side migration. Tracked separately under "Selective CLI Adoption of Shared Runtime" in `ROADMAP.md`.

## Cross-references

- Original sketch lived in conversation, not in repo. Phase 1 PR (#365) carries the canonical type definitions and design rationale in its body.
- Companion to `Architecture Remediation Plan — Defusing the Big Four.md`. The Big Four plan handles dispatcher / coordinator decomposition; this doc handles transport contract migration.
- The 12-arg `ProviderStreamFn` shape lives in `lib/provider-contract.ts` next to the new contract for now. Comparison is one file open away.
