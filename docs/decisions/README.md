# Decisions Folder

Status reviewed: 2026-06-03 (promoted Single-Agent Loop + Branch-at-Commit to ROADMAP first priority; indexed the Coder Delegation Collapse doc; prior 2026-06-01 sweep added the Role Display De-emphasis entry after PR #758)

This folder contains architecture decisions, research, analysis, and shipped design references.

## How to use this folder

- Prefer the docs marked **Current** when choosing new work.
- Treat **Historical** docs as context and provenance, not open task lists.
- Treat **Draft** docs as design-in-motion; implementation commitments still require a `ROADMAP.md` entry.
- Treat **Superseded by `<doc>`** as historical-with-pointer: the file stays for audit trail, but the canonical version lives at the named doc. Do not edit superseded docs; edit the successor.
- Treat **Merged into `<doc>`** the same way as Superseded, but signals the older doc's content was folded into the newer one rather than rewritten from scratch. Same rule: edit the successor, not the source.
- If a doc conflicts with the code, prefer the code and refresh the doc.

## Document Status

| Document | Status | Notes |
|---|---|---|
| `Addressable Session Verbs.md` | Draft, ROADMAP-tracked, added 2026-05-30 | Pins one canonical spelling for the session-lifecycle verbs Push performs internally (`abort`/`revert`/`summarize`/`children`), the layer above [`Universal Session Bearer.md`](Universal%20Session%20Bearer.md) (bearer → addressable verbs → optional session API) and suggested-priority #2 of [`opencode SDK Review.md`](opencode%20SDK%20Review.md). Decisions: **doc + thin executable aliases** (where genuinely daemon-reachable), **hybrid naming** (Push's shipped names stay canonical; new opencode-aligned names for internal-only verbs), **children modeled as addressable child sessions**. Audit reality-check: only `abort` is thin (and `cancel_run`'s session-ful path has an **auth gap** — no `validateAttachToken`, the 12th enforcement site); `summarize` needs a `lib/` promotion; `revert` needs a real checkpoint/truncate build; `children` is ~80% there. Phased sequence in the doc. |
| `Agent Experience Wishlist.md` | Historical | Shipped 2026-02-19; useful for provenance and feature rationale. |
| `Agent Tool Patterns — Claude Code Cross-Reference.md` | Reference, refreshed 2026-03-30 | Comparative design notes; several February borrow items are now shipped or partial. Remaining CLI follow-through is narrower memory adoption and later task-graph/product decisions. |
| `AgentScope Architecture Review.md` | Current, refreshed after tracing pass | Web-side OTel spans and sandbox-provider abstraction are now in place; main remaining work is Worker/server propagation and provider parity hardening. |
| `Architecture Rating Snapshot.md` | Reference snapshot, refreshed 2026-05-01 (all three reviewers reassessed) | Three-way architecture rating panel (Codex 8.6, Claude 8.7, Gemini 8.9; blended 8.7) plus synthesis. State-update bullets at the top track changes between full panel refreshes — the chronological structure means readers should scan top-down for current state. |
| `Architecture Remediation Plan — Defusing the Big Four.md` | Mostly shipped, refreshed 2026-05-14 | Three of the four modules discharged: `sandbox-tools.ts` closed the extraction at 670 lines, then later Local PC/Remote daemon work grew it again (971 lines on 2026-05-14) without undoing the handler-context split; `useAgentDelegation.ts` (1,883 → 673, −64%); `useChat.ts` (1,733 → 892, −48% by 2026-05-01). `coder-agent.ts` is structural per Step 5 (1,989 → 1,411 after 2026-05-01 helper extraction). Status field inside the doc is a multi-paragraph chronicle — needs a collapse pass; the chronicle is load-bearing context but obscures current state. |
| `Auth Rework — GitHub as the Single Identity Anchor.md` | Draft, added 2026-06-04 | Login is doing three mismatched jobs (edge gate via the shared `X-Push-Deployment-Token`, identity, repo-auth). Decision: anchor identity **and** repo-authorization on GitHub — GitHub OAuth + an allowlist of authorized GitHub user IDs (one entry, single-user) replaces the deployment token; App **installation** tokens become the default repo-auth (dissolving the PAT-into-sandbox `needs_ack` wart), PAT demoted to escape hatch. Real risk being gated is **cost/abuse** of metered Workers AI + sandbox compute, not data. Identity + installation-token anchors are audience-independent; only the gate is single-user-specific. CF Access is the edge-level runner-up (clunky on APK/daemon). Needs a `ROADMAP.md` entry. |
| `CLI Prompt Builder Convergence.md` | Implemented core path, refreshed 2026-04-05 | Shared `SystemPromptBuilder` lives in root `lib/`; CLI prompt pipeline consumes it. |
| `Claude Code In-App Patterns — Lessons For Push.md` | Partly shipped 2026-05-15 | Addendum to the 2026-02 cross-reference. Eight patterns from `code.claude.com/docs` (subagent isolation, hooks, deny-first permissions, lazy skills, MCP tool deferral, Agent View, auto-compaction, drift-detector tests) with current Push state and a load-bearing next step per pattern. #1 (PR #561), #2 (PR #562 + #563), #7 (this round — `lib/context-summary.ts` shared summary primitive + `lib/compaction-tiers.ts` typed tier interface) shipped. Remaining suggested ordering: 5 → 4 → 6 → 3. |
| `Cloudflare AI Gateway Integration.md` | Shipped v1 2026-04-25 | OpenRouter chat + Workers AI binding routed through CF AI Gateway for observability/caching. Not a router (provider/model selection still owned by chat-lock + role-context layer). Universal endpoint, Vertex/Azure/Bedrock, and non-CF-catalog providers (Zen, Kilocode, etc.) remain out of scope here — see `Provider Observability via Analytics Engine.md` for the parallel-track observability path that covers them. |
| `Cloudflare Artifacts.md` | Draft, added 2026-05-14 | Records why we evaluated `cloudflare/artifact-fs` for the cf-sandbox cold-start path and chose not to adopt it: the CDN payoff is gated on Cloudflare Artifacts (storage product, closed beta), the `cp -al` hardlink cache breaks on a FUSE workspace, and we have no per-phase ready-state data yet to know whether `clone` is even the bottleneck. Branch `claude/add-cloudflare-artifacts-QSk0x` shipped the instrumentation (`cf_sandbox_create_timing`) + `depth: 1` shallow clone that the decision depends on. Two explicit triggers documented for promoting this draft to an implementation decision. |
| `Cloudflare Native Backup Migration.md` | Draft, added 2026-05-25 | Records the decision to migrate the CF snapshot impl from the hand-rolled `tar.gz`→base64→R2 path (#647–#651) to the SDK-native `createBackup`/`restoreBackup` (squashfs + presigned R2 + FUSE-mount restore), which removes the 32 MB Worker-memory ceiling. Contained two-function swap behind `SandboxProvider`; routes/DO-resume/Modal-parity unchanged. Beta dependency, tolerable at single-user blast radius. Needs a `ROADMAP.md` entry + a graduation trigger (32 MB ceiling bites, or opportunistic CF-sandbox work) to implement. |
| `Cloudflare Sandbox Provider Design.md` | Shipped 2026-04-20 (PRs #353–#356) | Cloudflare Sandbox provider sits behind the existing `SandboxProvider` interface alongside Modal. Selection via `PUSH_SANDBOX_PROVIDER`. Owner-token auth, in-container streaming reads. Snapshot parity (R2-backed tar.gz) remains a follow-up; GPU stays Modal-only. |
| `Coder Bypass of WebToolExecutionRuntime.md` | Current, added 2026-05-14 | Explains why the web Coder path stays on `lib/coder-agent-bindings.ts` instead of routing through `WebToolExecutionRuntime`. Closes the deferred follow-up from PR #546: the runtime's interactive-pipeline features (approval gates, Protect Main, pre/post hooks) conflict with autonomous-delegation semantics, the DO surface uses the same bindings (so unifying Web only would be asymmetric), and the kernel role check is inlined at the top of `buildCoderToolExec` for both surfaces. The bypass is intentional, not a TODO. |
| `Coder Delegation Collapse — Component Audit.md` | ROADMAP-tracked (first priority, sequenced first, promoted 2026-06-03); impl pending | Audits the Orchestrator→Coder delegation surface to find what's safe to remove now that a frontier model leads. Premise: delegation existed for cheap context + small-model focus — both expired (the first is now a *display* problem solved by `Role Display De-emphasis.md`/#758; the second is moot). **Keep the engine, collapse the wrapper:** the durable job engine (`coder-job-do.ts` `ctx.waitUntil`, `Last-Event-ID` replay, checkpoints, bounded resume) and the snapshot are load-bearing and separable from delegation (a raw user turn already runs through the engine with no Orchestrator/Planner via `startMainChatJob`). Five-category classification: **(1) display-only** + **(2) Planner/brief ceremony** = the cut targets; **(3) event-shape compat** (`subagent.*`/`task_graph.*`, v1↔v2 downgrade, drift pins), **(4) runtime safety boundary**, **(5) progress/liveness** = PROTECTED. End state: the lead agent drives the engine directly. **Pairs with `Main as Scratchpad — Branch on Graduation.md`** (same frontier-lead-cut-the-wrapper move; its `auto-branch-on-commit` is the durability story for the collapsed single-agent loop). Category 4 carries the **Auditor reconciliation** (2026-06-03): safety *function* protected, model-Auditor *implementation* unbundles per the scratchpad doc's open-Q #2. **Promoted to ROADMAP first priority 2026-06-03** (sequenced first); cut category 2 behind a flag and measure vs. the delegated path before deleting. |
| `Context Memory and Retrieval Architecture.md` | Current, added 2026-04-05 | Concrete follow-up design for typed, invalidation-aware memory retrieval and prompt packing. |
| `Lossless Verbatim Memory Retrieval (LCM).md` | Current for Phases 0–2 (shipped 2026-06-01); Phase 3 Draft | Extends [`Context Memory and Retrieval Architecture.md`](Context%20Memory%20and%20Retrieval%20Architecture.md) with the LCM "recall the exact original" escape hatch (ref: Voltropy arXiv 2605.04050; the wild `pip install openlcm` is an unverified package we explicitly do **not** adopt). Documents two gaps: the packer never reads `record.detail` (write-only — contradicts the parent doc's "Prompt Packing Model"), and there's no model-facing retrieval tool. **Phase 0 shipped:** deterministic `lib/context-memory-expand.ts` kernel (`expandMemoryRecords` / `grepMemory`, substring-not-regex, 12 vitest cases). **Phase 1 shipped 2026-06-01:** packer surfaces top-ranked `detail` behind the opt-in `MemoryPackOptions.includeTopDetail` flag (default off, summary-only fallback on overflow); parent doc's "Prompt Packing Model" note flipped to shipped. **Auditor opt-in shipped** on both surfaces (web `role-memory-context.ts` + CLI `auditor-gate-memory.ts`) via shared `AUDITOR_MEMORY_PACK_OVERRIDES`. **Phase 2 shipped 2026-06-01:** model-facing `memory_grep`/`memory_expand` tools — new `memory` source + `memory:read` capability (all five roles), shared `lib/memory-tool-exec.ts`, both surface executors (scope injected from session context, never model args), packer leads record lines with `[mem_…]` ids, symmetric structured logs, advertised to Orchestrator + Explorer; drift test green. Remaining: Phase 3 optional true verbatim immutable log in `lib/`; broaden tool prompt-advertising to Coder/Reviewer/Auditor. Needs a `ROADMAP.md` entry to graduate Phase 3. |
| `Copilot SDK Research.md` | Current, partially superseded | Prompt sections, tool scoping, steering/queueing, and live-vs-persisted event split are now in place; main open carry-over is a dedicated task agent plus richer session/permission telemetry. |
| `CorrelationContext Contract.md` | Current, docs-and-types only, added 2026-04-14 | Step 1 of the remediation plan: canonical shape for passive cross-surface correlation tags. Propagation is step 3. |
| `Diff and Annotation Envelope.md` | Draft, added 2026-05-12 | Directional decision to hand-roll a `lib/`-canonical diff + annotation envelope consumed by web and CLI/TUI renderers, with `@pierre/diffs` and `modem-dev/hunk` studied as design references only. Forced by remote sessions (Reviewer/Auditor/Coder payloads have to ride the relay) and by Reviewer's GitHub PR round-trip needing a serializable annotation taxonomy. No implementation commitment yet — needs a `ROADMAP.md` entry to graduate. |
| `Duplication and Structural Symmetry Analysis.md` | Current, refreshed 2026-03-30 | `hashline` drift and shared provider-model catalog drift are resolved; remaining cleanup is Settings state surfaces and other mirrored modules. |
| `External Resource Review — Harness Engineering and Ralph Loop.md` | Reference | Source for the 2026-04-14 Tiered Orchestrator Routing and Canonical SOP Playbooks spikes. |
| `GitHub Token Storage — localStorage vs httpOnly Cookies.md` | Current, added 2026-05-02 | Decision to defer the localStorage→httpOnly migration flagged by the 2026-05-01 audit (#5). Captures threat model, current defenses (CSP, origin allowlist, 1h token TTL, server-side revocation via PR #465), explicit trigger conditions for revisiting, and a thin migration sketch. |
| `Goal-Anchored Task Graph Layering.md` | Current, added 2026-05-14 | Promotes the PR #549 `[USER_GOAL]` anchor from decorative to load-bearing. Per-node `addresses` field on `TaskGraphNode`, runtime validator (`validateTaskGraphAgainstGoal`) with structured rejection back to the model on missing fields, and `userGoal` + `addresses` rendered in the delegation brief so Coder/Explorer see the goal too. Web-only for v1; CLI planner parity is a separate piece of work. Also codifies the scratchpad-vs-`goal.md` split. |
| `Harness Friction — Agent Self-Report.md` | Current, refreshed 2026-03-30 | Ambient runtime state, capability discovery, and structured mutation postconditions shipped on main web path; working-memory evolution and richer structural navigation remain open. |
| `Hashline System Review.md` | Current scorecard, added 2026-04-08 | Recommendations A, C, D, E shipped; B shipped as a tool-level patchset range form. Effectiveness measurement logged 2026-04-12. |
| `Hermes Agent — Lessons For Push.md` | Historical, shipped 2026-05-16 in PRs #572 / #573 / #574 | Research note on `NousResearch/hermes-agent`. All three borrow items landed: (1) cache breakpoint audit — `system + rolling 3` — shipped PR #573 with `cache_control` preservation through `chat-request-guardrails` so web actually benefits; (2) typed `SessionDigest` for compaction output + `injectSessionDigestStage` + 85% gateway safety net (`safetyNetStage`) in `lib/context-transformer.ts`, wired into web orchestrator and CLI engine main loops; (3) skill frontmatter (`requires_capabilities`, `platforms`) + auto-hide in `cli/skill-loader.ts` — shipped PR #572. Out-of-scope items (monolithic AIAgent, provider fallback chain, Honcho user modeling) documented inline to prevent re-litigation. |
| `Loop Detection — Near-Duplicate Layer.md` | Prototype landed dark, added 2026-05-25 | Borrows Pi-forge's `loop-guard` similarity check. Push already breaks loops on exact `(tool, args)` keys in two divergent places (web `checkLoopBreaker` via the shared `MutationFailureTracker`; CLI's ad-hoc `repeatedCalls` batch map). Neither catches a model re-writing the same file with near-identical content each round. Prototype shipped the shared kernel (`lib/loop-detection.ts` + 33 unit tests) and converged BOTH surfaces onto it: CLI (deleted the inline `seen >= 3` breaker, drift test) and web (`checkLoopBreaker` now feeds `exactBreakers` to the oracle and threads a per-run `SimilarityLoopDetector`). Behavior preserved; near-duplicate ladder stays dark (`PUSH_LOOP_DETECTION=1`, logged-only). Verdict telemetry landed via `lib/loop-metrics.ts` (shared in-memory aggregator; both surfaces call `recordLoopVerdict` every turn; `darkSuppressed` = would-fire count), with CLI export to `~/.push/loop-metrics/verdicts.jsonl` (`cli/loop-metrics-store.ts`, per-invocation scope + run-end summary). Pending: graded warn/block/compact enforcement, web→Analytics-Engine export + threshold tuning, `system:loop-detection` capability gate, hashline/search-replace similarity coverage. Explicitly excludes Pi-forge's incremental-codegen scaffolding and thinking caps (frontier-model overkill). Needs a `ROADMAP.md` entry to graduate enforcement. |
| `Main as Scratchpad — Branch on Graduation.md` | ROADMAP-tracked (first priority, promoted 2026-06-03); impl pending | Pins the persistence posture behind Push's "talk on `main` without branching first" differentiator, in one line: **take the mandatory branch every cloud agent forces and move it from the front of the interaction to the point of intent.** Anchored on a concrete counterexample — another agent's cloud env branches *before* the first response and runs a stop-hook on uncommitted changes, because its persistence layer *is* git (no scratchpad floor), so the branch is the only durable home and must come first. Push's snapshot is a durability floor *below* git, which frees the branch to fire at **commit** (the moment intent appears) instead of up front. Model (revised 2026-06-03): `main`-first + cloud-default; **`auto-branch-on-commit`** — the first commit auto-creates a branch (no prompt, no choice, nothing ever lands on `main`) and auto-pushes it behind a secret scan, which *dissolves* the three problems an interactive "branch this?" prompt created (who-answers-esp-headless / does-"No"-commit / `Protect Main` collision). **Decomposition:** flag the *storage substrate* by platform (APK local-git/SAF vs PWA remote-snapshot), NOT the commit-flow — `auto-branch-on-commit` is universal, avoiding a two-mental-models tax. Key reframe vs the original draft: the snapshot is **best-effort warm-reattach, not a durability guarantee** — bar moves from *never lose work* to *fail loudly, never silently* (the `RESTORE_FAILED_MESSAGE` surface already does this). Notes sandbox keyed `(repo, branch)` not `chatId` (`sandbox-session.ts:59-66`), `create_branch`'s working-tree-carry already supplies the plumbing, and the `:main` single-slot contention is **live** for a bi-surface owner (Android + WSL) — candidate fix a per-device remote slot, not the original "it's a signal" hand-wave. The *where-the-delta-lives* fork (remote vs phone-local) is split into `Scratchpad Durable Storage — Remote vs Phone-Local.md` — now rescoped as the platform-flagged substrate, carrying two conscious-but-undecided tradeoffs: APK-local re-adopts phone-local and **narrows cross-surface continuity** to "graduate-to-a-branch," and an **identity** pick (scratchpad = APK power-user hook vs niche perk). Open-Q #2 now leans toward **unbundling the model-Auditor**: it was the v1 gate when commit-to-`main` was primary, but the new model relocates its three jobs to better-fitted homes — secrets → deterministic pre-push scan (`sensitive-data-guard.ts` is the seed), semantic judgment → the existing PR reviewers (Copilot/Kilo/glm-5.1), land-on-main → Protect Main; plus it *defaults UNSAFE on error* (blocks commits on a flaky backend). Keep a model-judge at graduation only if a "dangerous-AND-must-catch-pre-push-beyond-secrets" band proves non-empty. **Promoted to ROADMAP first priority 2026-06-03** (sequenced after the delegation collapse); cheapest proving slice is `auto-branch-on-commit` + auto-push + auto-naming. |
| `Modal Sandbox Snapshots Design.md` | Partially shipped, added 2026-04-14 | Phase 1 (backend primitives) shipped 2026-04-16 in `23fcb8e`; Phase 2 (auto-restore + idle hibernation timer) shipped 2026-04-16 in `f913f49` with follow-up hardening in `2613527`. Phase 3 (UX polish: explicit Hibernate/Forget controls, snapshot-age on resume banner, Settings list) and Phase 4 (eviction cron + KV index) still open. |
| `Multi-Agent Orchestration Research — open-multi-agent.md` | Reference, added 2026-04-04 | Research on open-multi-agent framework. Core task-graph/memory work shipped; keep for rationale. |
| `Oh My OpenAgent Review.md` | Comparative review | Tier-1/2/3 quick-win list for Push. |
| `OpenAI Agents SDK Evolution Review.md` | Reference | Comparative review of the OpenAI Agents SDK. |
| `opencode SDK Review.md` | Reference, added 2026-05-30 | Comparative review of the [opencode Go SDK](https://github.com/anomalyco/opencode-sdk-go) (Stainless-generated from an OpenAPI spec over a headless `opencode serve` daemon) against Push's `push.runtime.v1` contract. Contract-shape review, not a topology borrow — opencode's headless-server-as-center-of-gravity is the Phase 7 "Web-as-daemon-client" that `push-runtime-v2.md` holds out of scope. Finds Push already has the hard parts (version-pinned envelope, `seq`/`lastSeq` replay, drift-test contract guard, `approvalId` request/respond pair) but lacks a machine-readable description of the contract it enforces in TS. Top borrow: a JSON-Schema for the `push.runtime.v1` envelope + `SCHEMA_VALIDATED_EVENT_TYPES` payloads, wired into the protocol-drift suite (topology-neutral, additive). Secondary: pin session-verb vocabulary (`abort`/`revert`/`summarize`/`children`) doc-only. No implementation commitment — needs a `ROADMAP.md` entry to graduate. |
| `Phase 5 Handoff - Task-Graph Extraction.md` | Historical, shipped 2026-04-19 | Task-graph branch of `useAgentDelegation` extracted to `lib/task-graph-delegation-handler.ts` (760 lines); hook shrunk 1,213 → 490 (−60%). Option A (preserve single/multi-coder-node `evalWorkingMemory` policy byte-for-byte) selected at design spike. Companion to `useAgentDelegation Coupling Recon.md`. |
| `phase-5-tool-runtime-brief.md` | Historical, shipped 2026-04-12 in `6fecefcc` | The brief's "Draft pending review" status was wrong: implementation landed in commit `6fecefcc` the same day as the brief itself. `lib/tool-execution-runtime.ts` (interface), `app/src/lib/web-tool-execution-runtime.ts` (Web adapter), and `executeAnyToolCall` thin shim all match the brief's acceptance criteria. Phase 5C (deep-reviewer) and Phase 5D (Explorer, Coder) followed — see `push-runtime-v2.md`. Brief preserved for design rationale. |
| `pi-mono Agent Loop Review.md` | Current, added 2026-04-27 | Comparative review of `badlogic/pi-mono` `packages/agent`. Top borrows: kill mid-conversation working-memory re-injection, source-order thunk pattern in parallel-reads, proxy event-stripping wire format between Worker and app. |
| `Provider Observability via Analytics Engine.md` | Shipped v1 2026-04-29 | Workers Analytics Engine dataset + read endpoint covering every provider call, gatewayed or not. Closes the gap CF AI Gateway can't (six non-CF-catalog providers: Zen, Kilocode, OpenAdapter, Nvidia, Blackbox, Ollama Cloud). LiteLLM was the obvious self-hostable answer; April 2026 breach took it off the table. |
| `push-runtime-v2.md` | Complete for durable v2 scope | Phases 1–6 shipped; Orchestrator remains Web-side by design. Runtime-schema validator landed 2026-04-14 and later moved to `lib/protocol-schema.ts` for Local PC / Remote consumers. |
| `PushStream Gateway Migration.md` | Complete, shipped 2026-04-25 | Nine-phase migration from the 12-arg `ProviderStreamFn` callback to `PushStream<M>` async-iterable transport. Every consumer (chat UI, agent roles, CLI daemon, CLI REPL, worker coder-job) iterates `PushStream<M>` events directly; both bridges, all legacy SSE machinery, and the CLI shims have been deleted. CLI's `streamCompletion` is now a thin retry/timeout wrapper around the same `openAISSEPump` the web side uses. |
| `Repo Mirror Design.md` | Proposed, council-reviewed 2026-05-01 | "AI-aware mirror, not a GitSync replacement." Manifest-driven diff-aware sync with content-addressed SHAs targeting Android SAF folder + zip-bundle fallback. Council review (Gemini + Codex 2026-04-30) reframed the strategic positioning, expanded the manifest schema, added explicit handling for tree truncation / submodules / symlinks / LFS / SAF revocation / IndexedDB eviction. ~2.5 days estimated for v1. |
| `Remote Control Surface Audit — TUI and Daemon Exposure.md` | Reference, findings snapshot, added 2026-05-31 | Audits what's *exposed* across the remote-control surface (vs the verb vocabulary, which [`Addressable Session Verbs.md`](Addressable%20Session%20Verbs.md) owns). Headline: the daemon's bearer-gated addressable verbs have outrun the TUI's exposure of them — the TUI *reacts* to `session_reverted`/`unreverted`/`context_compacted` (resync) but never *sends* `session_summarize`/`session_revert`/`list_children`/`get_child_session`; only `cancel_run` is TUI-initiable (Ctrl+C). Also: PR delivery in `local-daemon` mode (originally called "dead" — **corrected 2026-05-31**: the strip at `lib/capabilities.ts:365` has a `remoteGitHubAvailable` escape hatch the CLI already threads, so it works with a `gh`/env token — granted + advertised, verified deterministically; residual is soft prompt under-guidance, not a contradiction), cross-phone cancel isn't identity-scoped (`pushd.ts:363`), delegate audit is coarse (no `delegate.complete`), and three capability docblocks (`pushd.ts:4416`/`4067`/`4423`) are stale (explorer + `multi_agent` already shipped). Suggested sequencing inside. **#1 shipped 2026-05-31** (TUI `/revert`, `/unrevert`, `/children`, daemon-aware `/compact`); verification surfaced finding #6 (**fixed 2026-05-31**: not a stale-`dist` *spawn* — the spawn mirrors the TUI's surface — but silent *reuse* of a pre-existing daemon whose in-memory code predates current source; now warns via a pidfile-mtime-vs-source check on the reuse path). Remaining items need a `ROADMAP.md` entry to become work. |
| `Remote Sessions via pushd Relay.md` | Phases 1-3 shipped, Phase 2.j daemon-shell convergence landed 2026-05-28, refreshed 2026-05-28 | Directional decision for phone/PWA/APK remote editing of files on a user's PC: `pushd` remains the local execution host, a Worker/Durable Object relay forwards runtime envelopes, and any desktop wrapper is packaging/pairing polish rather than a separate runtime. Local PC and Worker-relay sessions are end-to-end testable from chat: `sandbox_exec`, `sandbox_read_file`, `sandbox_write_file`, `sandbox_list_dir`, and `sandbox_diff` route through the daemon over the hook-owned WebSocket. Phase 2.j (#668–#673) collapsed the daemon shell onto the standard workspace shell: `DaemonHubSheet` + standalone `DaemonModelPicker` deleted, three shared hooks (`useDaemonAppearance`, `useDaemonSettingsBundles`, `useDaemonCliSessions`) own the per-transport affordances, full `ChatInput` adopted, session `mode` persisted on `list_sessions`. Remaining scope is Phase 4 desktop-wrapper polish. |
| `Remote Session Status Packet.md` | Current, Slice 1 shipped 2026-05-31 | Defines a bearer-gated `get_session_snapshot` daemon read RPC as the reconnect baseline for TUI/mobile/remote surfaces: host, repo, relay, session, active run, pending approval, and recent events in one daemon-owned packet. Slice 1 is daemon contract only; TUI/mobile hydration follows after the packet proves stable. |
| `Rerank Before Prompt Packing.md` | Draft spike, added 2026-04-14 | Optional rerank stage between deterministic retrieval and sectioned packing for delegation briefs and Auditor. |
| `Resumable Sessions Design.md` | Historical | Shipped 2026-02-19 (Phases 1–4); useful as provenance and for journal-adjacent designs like Modal Sandbox Snapshots. |
| `Role Display De-emphasis.md` | Current, shipped 2026-06-01 in PR #758 | Records the four-layer split between internal runtime role, event/log attribution, display vocabulary, and user-facing presentation. `lib/role-display.ts` is the single source of truth for human-readable role/phase labels: Explorer/Coder render as phases, Orchestrator source attribution renders as Assistant, Reviewer/Auditor keep names where independent attribution improves trust, and raw role strings stay in runtime contracts/logs/persistence. |
| `Sandbox Policy Seam.md` | Current, added 2026-05-11 | OpenShell-evaluation arc (#502/#503/#504). Schema + provider contract + host-side enforcement on `sandbox_exec`'s git guard. Native provider compilation deferred — open items have no consumer yet and explicit triggers documented. |
| `Scratchpad Durable Storage — Remote vs Phone-Local.md` | Draft, added 2026-06-03, rescoped 2026-06-03 | Split out of `Main as Scratchpad — Branch on Graduation.md`, then **rescoped** by that doc's decomposition: commit-flow is settled (`auto-branch-on-commit`, universal), so the **platform flag governs only the durable-storage substrate** — which is what this doc owns. The "where does the uncommitted `main` exploration durably live" question: remote snapshot (R2/Modal) vs phone/device-local (owner-held delta → stateless container) vs hybrid. Stakes lowered by the parent's best-effort snapshot contract (any option clears a best-effort bar). For *this* owner the binding constraint is bi-surface continuity (Android + WSL), which favors **remote-snapshot-primary with a per-device slot** (device-agnostic + fixes `:main` contention) over the original phone-primary recommendation (caveat #1, single-device, would sever the bi-surface workflow). Carries the trichotomy table, the three caveats (single-device / device-is-loss-surface / APK-strong-PWA-weak), and the `Repo Mirror Design.md` substrate dependency. Escalates if best-effort stops being good enough or reachability gets solved. |
| `Sectioned System Prompts.md` | Shipped design reference | Sectioned system prompt builder refactor. |
| `Streaming UI Deltas.md` | Draft, scoping-validated 2026-05-01 | Proposal to extend the existing `tool.execution_start` run event with optional display metadata (icon, subtitle, approval affordance) so the chat surface can render a `pending-tool` placeholder card at dispatch and swap it for the real typed card on completion. Borrowed shape only from the third-party Claudio APK reverse-engineer; sourcing caveat documented in-doc. Estimated 2–4 hours after PR #463 review fixes simplified the design (no binding-layer plumbing, no parallel event). |
| `Subprocess Env Scrubbing.md` | Current, shipped 2026-05-28 in PR #662 | Default-deny env allowlist (`cli/env-scrub.ts`) wired into every model-invoked CLI subprocess (`sandbox_exec`, `push run` checks). Closes the leak where a model could read provider keys + GitHub PATs from the daemon's hydrated `process.env` via `env`/`printenv`. Documents the allowlist categories, the platform-normalization quirk for Windows, the three extension layers (per-call, `PUSH_SCRUB_ALLOW`, `PUSH_SCRUB_DISABLED` bypass), and the four CLI call sites. No equivalent wiring on web — sandbox provider isolates at the container boundary. |
| `Tool-Call Parser Convergence Gap.md` | Resolved 2026-05-28 | Both CLI and Web route through the shared `createToolDispatcher` kernel at `lib/tool-dispatch.ts`. CLI side resolved 2026-04-15 + 2026-04-18 (PR #334). Web side resolved 2026-05-28 in a six-PR tranche: #677 (phase-grouping kernel), #679 (web onto kernel + textual-order merge), #680 (cap unification + `batchOverflow` split + CLI per-code rejection), #681 (kernel `enableInternalRecovery: false` + recovery-args-region gate), #683 (added `RecoveredNamespacedCall.endOffset` / `RecoveredXmlCall.endOffset` / `ToolDispatchResult.callOffsets` APIs and replaced two heuristics — kernel-call offset reconstruction + `<function_calls>` invoke offset re-anchor — with exact data; the legacy-scan recovery-args region skip stays shape-based by design because recovery rejects prose mentions on trailing context, leaving args still bare-args-inferrable), #684 (web user-facing error surface aligned with the kernel split — distinct `FILE_MUTATION_BATCH_OVERFLOW` vs `MULTI_MUTATION_NOT_ALLOWED` codes + closed the silent-drop regression where batch overflow could disappear without a model-facing error). Two follow-ups (per-source `ToolSource` split, bare-block-eligibility loosening) deferred without forcing function — see in-doc notes. |
| `TUI Decomposition - Testability Seam and Daemon Session Controller.md` | Current — Phase 0 shipped 2026-06-01 (ROADMAP-tracked); Phases 1–2 planned | Plan for decomposing the ~6,500-line `runTUI` closure, seeded by PR #740's declined piecemeal-extraction note. **Phase 0 shipped:** `options.io`/`options.deps` seam (process/IO + daemon-client factory + config/session loaders) + `cli/tests/tui-driver.mjs` headless harness + characterization tests for the #740 verbs via the real dispatch→send path. Key framing: leaf concerns (render/input-parse/daemon-helpers) are *already* in `tui-*` modules; what's left inline is orchestration — ~11 command handlers + the daemon-session lifecycle (`daemonClient`/`daemonSessionId`/`daemonAttachToken` read across ~90 sites, assigned at 12). **Net-before-cut ordering:** Phase 0 = IO/dep injection through the existing `options` param + a headless `tui-driver` harness with a stub daemon client + characterization tests (which retroactively close the verb-test gap punted in #740, exercising the real dispatch→send path); Phase 1 = extract `cli/tui-daemon-session.ts` `DaemonSessionController` owning the state+lifecycle+verbs (audit finding #7's typed payloads ride here); Phase 2 (optional) = wholesale command-handler module. Not a `lib/` promotion (TUI-shell-specific); home is `cli/`. Needs a `ROADMAP.md` entry to implement. |
| `Universal Session Bearer.md` | Draft, added 2026-05-30 | Every daemon session carries an attach token from birth; every attach requires it ("open" becomes an explicit opt-in). Removes the tokenless-session class behind the 2026-05-30 `MISSING_ATTACH_TOKEN` remote-pairing failure and retires the PR #714 mint-on-demand patch. Decisions made: **Universal bearer (A)** + **bootstrap-grace** legacy cutover (a tokenless session's first no-token attach is a logged one-time claim, then enforced). Grounded in a full audit (3 creation points, 11 enforcement sites, per-client token sources, the TUI/CLI stale-in-memory lockout risk). **ROADMAP-tracked** under Session Continuity & Stability (first priority, 2026-05-30); paired with [`opencode SDK Review.md`](opencode%20SDK%20Review.md) #2 as its addressable-session sequel. |
| `useAgentDelegation Coupling Recon.md` | Historical, track complete 2026-04-19 | Five-phase extraction track shipped (PRs #335–#339), hook reduced 1,883 → 490 lines (−74%). Five handler modules landed in `lib/`. All 25 characterization tests green at HEAD. Coupling-hazards analysis preserved below the divider for future readers. |
| `useChat Regression Audit.md` | Recon doc, re-extraction track complete 2026-05-01 | The hook regrew from a 770-line refactor baseline (2026-03-25) to 1,733 lines (+125%). Audit identified the three top drift commits (~85% of regrowth). Re-extraction track shipped in two waves: Phases 1–4 (PRs #340–#343, 2026-04-19) reduced to 1,365; Phases 5–9 across 2026-04-26 → 2026-05-01 reduced to **892 lines** (−48% from peak). ESLint `max-lines` cap ratcheted 1,700 → 1,400 → 1,060 → 950. See `Architecture Rating Snapshot.md` 2026-05-01 state-update for the full chronology. |
| `Webhook-Triggered PR Review.md` | Current, ROADMAP-tracked (refreshed 2026-05-28) | Autonomous, webhook-driven PR-review trigger (baloo-bear reference). **Shipped:** receiver `github-webhook.ts` (HMAC verify, fail-closed secret + allowlist, event/draft filter, 202 enqueue; deployment-token-exempt) + `PrReviewJob` DO (per-PR SQLite, replay dedupe, coalesce-by-head-SHA, advisory-only `COMMENT` post with 422→body fallback, REVIEW.md at base ref, injectable executor) + `v4` migration (#690); REVIEW.md resolver promoted to shared `lib/review-guidance.ts` and the **CLI Reviewer resolves REVIEW.md by default**; token-injectable `github-tools` so the DO reuses the browser client, non-retrying review POST (#691); **PWA read-only review-history** — DO persists the full `ReviewResult` + `list` action, `/api/pr-reviews` route, `usePrReviewHistory` polling, `PrReviewHistorySection` in the review tab. Graduated from Draft via the `Autonomous Webhook PR Review` ROADMAP entry. **Not live until** the `v4` DO migration is applied via a one-time non-versioned `wrangler deploy`. Remaining: `runDeepReviewer` upgrade, re-run-from-PWA, abort propagation, severity→`REQUEST_CHANGES` gating (per-repo opt-in). |
| `Vercel Open Agents Review.md` | Current, added 2026-04-14 | Adoption target is sandbox snapshots; secondary targets are sandbox port exposure, read-only share links, and server-side durable runs on Cloudflare. |
| `Web and CLI Runtime Contract.md` | Current, refreshed 2026-05-14 | Architecture rule for Push shells: share agent-runtime semantics across web and CLI, allow divergence in transport and UX shell. Updated for `lib/protocol-schema.ts` ownership after Local PC / Remote shipped. |

## Quick Triage

If we are choosing implementation work from this folder, the best live clusters are now:

1. **Verification-family extraction proof in `sandbox-tools.ts`** — the synthesis-named landing for a 9+ implementation rating. Three of the Big Four are discharged; this is the specific test of the handler-context pattern across tool families. See `Architecture Rating Snapshot.md` 2026-05-01 synthesis.
2. End-to-end tracing follow-through (Worker/server propagation, exporter rollout, metric retirement).
3. Working-memory evolution and invalidation-aware investigation state, with a concrete follow-up in `Context Memory and Retrieval Architecture.md`. Note: pi-mono review (2026-04-27) recommends killing mid-conversation working-memory re-injection — worth checking against Push's current re-injection logic.
4. Sandbox snapshots Phase 3 (explicit hibernate/forget UX in Workspace Hub, snapshot-age on resume banner, Settings "Hibernated sandboxes" list) and Phase 4 (eviction cron + KV index). Modal snapshot Phases 1–2 landed 2026-04-16; Cloudflare snapshot parity remains a separate provider-parity item under `Cloudflare Sandbox Provider Design.md`'s "Non-goals."
5. **`Repo Mirror Design.md` v1** if AI-aware mirror is the next product priority — council-reviewed and ready for ~2.5 days of implementation.
6. **`Architecture Remediation Plan` collapse pass** — the doc's Status field is a multi-paragraph chronicle of 20+ updates since 2026-04-14. The chronology is load-bearing context but the current state is hard to find without scanning the whole paragraph; worth a one-pass collapse with supersession markers.
7. Selective CLI adoption of the shared runtime substrate where it clearly improves the terminal product.
