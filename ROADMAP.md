# Push Roadmap

Last curated: 2026-07-05

This is the high-level priority list, **not** the live decision surface. The
canonical, continuously-updated record of runtime and platform decisions lives
in the decision docs — `docs/decisions/Agent Runtime Decisions.md` and
`docs/decisions/Platform, Sessions, and Sandbox Decisions.md` — each carrying
its own `Status:` / `Reviewed:` date. The priority rows below can lag those
docs and the merged work; when they conflict, the decision docs and the code
win.

`docs/` is the working decision surface. Promote an item into this file only to
flag it as a committed near-term priority; treat the decision docs as canonical
for what has actually shipped.

Current cycle emphasis: **Single-agent loop + branch-at-commit persistence is the first priority** (promoted 2026-06-03; resumed 2026-06-11 after Durable Runs — promoted over it 2026-06-10 — shipped end-to-end #870–#884, landing step (1)'s inline flag and its A/B measurement along the way) — with a frontier model leading, the scaffolding built for weak models and for git-only durability is overhead. Collapse the Coder-delegation wrapper so the lead drives the durable job engine directly, and make the branch automatic at the first commit ("talk on `main`" → `auto-branch-on-commit` + auto-push behind a deterministic secret scan → best-effort snapshot for uncommitted scratch only). Keep the engines (durable job DO, snapshot, git), cut the wrappers (Planner/brief, branch-first ceremony, the model-Auditor). **Session continuity & stability remains the active north-star beneath it** — the north star is "start a session in the TUI and continue it on the phone (and vice versa)" with no manual state surgery. Everything that makes a session a durable, portable, authenticated thing serves this: the relay, the Universal Session Bearer (auth by construction), legible failure surfaces, and deterministic resume. Secondary, ongoing emphasis: transcript-first CLI ergonomics, selective CLI adoption of the shared runtime foundation, and chat/workspace product follow-through. **Push CLI Muscle-Memory UX is also load-bearing for the Architecture Remediation Plan's evaluation gate** — extraction validation depends on real-use observations, which require the CLI to be daily-driver ready (see `docs/archive/decisions/Architecture Remediation Plan — Defusing the Big Four.md §CLI Runtime Parity` for the source framing). Treat CLI usability as equivalent-priority to extraction, not adjacent. Live decision summaries now live in `docs/decisions/Agent Runtime Decisions.md` and `docs/decisions/Platform, Sessions, and Sandbox Decisions.md`.

## How We Use This

1. Draft ideas in `docs/` (untracked is fine).
2. Promote approved work here as concise, actionable items.
3. Keep this file current during execution.
4. Archive completed/abandoned items out of this file to keep it focused.

## Status Legend

- `planned` - approved but not started
- `in_progress` - actively being implemented
- `blocked` - waiting on dependency/decision
- `done` - completed and verified

## Current Priorities

| Item | Status | Scope | Acceptance Criteria |
|---|---|---|---|
| **Durable Runs — Adopt-on-Silence** — *promoted 2026-06-10* | done (phases 0–3 shipped 2026-06-10/11: #870–#878, hardening #879–#884; decision doc Status: Current; delegation-collapse A/B measured 2026-06-11 — direct ≥ delegated, `docs/measurements/delegation-collapse-ab/`) | The web round loop is browser-resident, so a run dies with the tab — on a mobile-first product, the core promise carries an "as long as the screen stays on" asterisk, made sharp by Background Exec (#861–#867: the sandbox half of a run is now durable, the agent half is not). Decision: **local while watched, adopted when away** — the in-page loop is preserved exactly while a client is attached (zero responsiveness regression); per-turn checkpoints feed a `RunHost` DO that adopts a mid-flight run when client heartbeats lapse and continues it server-side on the same host-agnostic `lib/` kernels (the dual-home pattern the coder kernel already proves); reopening attaches via bearer + snapshot + run-event cursor, with pull-back-local. **Phased:** **(0)** instruments — a DO-relay latency spike (TTFT + per-turn overhead from a phone, numbers before architecture) AND the agent eval harness (~10–20 scored repeatable tasks; also supplies the measurement the delegation-collapse A/B has been missing); **(1)** `RunCheckpoint` schema in `lib/` + drift pin, extending Resumable Sessions to mid-run fidelity; **(2)** delegation collapse lands behind a flag as the precondition (simplify the loop AS it goes host-agnostic), then the `RunHost` DO with adoption semantics (full-auto continues; supervised pauses at gates), CoderJob/PrReviewJob alarm-hygiene lessons applied; **(3)** attach/viewer with stop/approve/pull-back controls. Absorbs Single-Agent Loop step 1; Session Continuity track (c) largely merges in at Phase 3; Pre-Order PRs becomes a thin RunHost special case later. Decision doc: `docs/decisions/Durable Runs — Adopt-on-Silence.md` | Start a multi-tool run on the phone, lock it mid-run, reopen after the adoption threshold: the run continued server-side from checkpoint with a complete transcript across the gap; supervised approval gates paused (not proceeded) while adopted; a tab-open run behaves exactly as today; Phase 0 numbers recorded before Phase 2 is built |
| **Single-Agent Loop + Branch-at-Commit Persistence** — *promoted 2026-06-03; step (1) shipped behind a flag in Durable Runs Phase 2; A/B measured 2026-06-11 — direct ≥ delegated on every axis (`docs/measurements/delegation-collapse-ab/`); inline default FLIPPED 2026-06-11; **Inline Foreground Lane shipped 2026-06-11/12** (#892/#893 + follow-ups #894–#904: inline = the coder kernel runs in-browser as the lead — streaming, memory tools, RunHost adoption — with the engine route narrowed to the explicit background-mode toggle; doc: `docs/archive/decisions/Inline Foreground Lane — Local While Watched.md`, Archived); **CLI converged 2026-06-12** (#903–#905: terminal turns default to the same shared kernel in `leadMode` via `cli/lead-turn.ts`, `PUSH_LEAD_RUNTIME=engine` opt-out while it bakes — Agent Runtime Decisions §10 steps 1–3); step (1) remainder **SHIPPED 2026-06-15**: Planner/brief deleted (#940), CLI engine loop's duplicated round machinery retired (#942/#943 — every CLI turn now runs the shared kernel), and the multi-image-history fidelity gap that gated **web Orchestrator-loop retirement** is closed (#937 current-turn attachments + #938 prior-turn attachments across the inline AND background lanes — prior-turn images now ride the same job-chain walk as the summaries, no client/server drift). **Next step-(1) frontier: web Orchestrator-loop retirement** — `toLLMMessages` / the delegated org-chart round loop in `app/src/lib/orchestrator.ts` is now the only path the inline lead doesn't yet fully subsume; it is unblocked but not started* | in_progress | Collapse the weak-model / git-only scaffolding now that a frontier model leads the slot, keeping the durable engines. **Sequenced:** **(1)** collapse Coder delegation behind a flag — stop forcing `delegate_coder` → Planner → brief; let the lead edit inline (or use the background-main-chat entry for detached runs) and *measure quality+latency vs. the delegated path before deleting* the Planner/brief. **(2)** `auto-branch-on-commit` — **SHIPPED for web/cloud 2026-06-13** (#921 doc → #922 impl; `docs/decisions/Auto-Branch on Commit — Nothing Lands on Main.md`, Current): committing while on the default branch forks to a model-named (deterministic-fallback) branch first via the typed path so nothing lands on `main`, the chat follows (`kind:'forked'`), and the push is gated by the pre-push secret scan — which also closed a pre-existing raw-push gap on the workspace hub. One shared `ensureCommitTargetBranch` seam across the model tool path + web commit surfaces, behind `PUSH_AUTO_BRANCH_ON_COMMIT` (default-on). Builds on the typed-path branch governance (Branch Transition Governance, below). **Remaining for this step:** the snapshot-contract demotion to *uncommitted `main` only*, and the platform storage-substrate flag (folded into step 4). **CLI/daemon auto-branch is decided *against* (2026-06-14):** auto-branch's load-bearing justification is durability against an ephemeral sandbox — absent on a real local filesystem, where committed work is already durable on disk; the "don't clobber `main`" safety function is already served by the shared Protect Main pre-hook (`createProtectMainPreHook`, registered at `cli/tool-hooks-default.ts`), cross-surface reachability rides the pushd relay (not a pushed branch), and auto-forking local commits breaks the Claude Code/Codex muscle-memory parity the CLI track targets. This revises the 2026-06-03 "flag the substrate, not the commit-flow" framing: keeping a universal commit-flow for consistency *after* its load-bearing reason is gone is exactly the ceremony this track exists to cut. The pre-push secret scan stays on any CLI surface that pushes (orthogonal to branching). **(3)** unbundle the model-Auditor → secret-scan + the existing PR reviewers + Protect Main (function protected, implementation relocated). **(4)** platform-flag only the *durable-storage substrate* (APK local-git/SAF vs PWA remote-snapshot), gated on two conscious tradeoffs: cross-surface-continuity narrowing + the identity pick. Runtime summary: `docs/decisions/Agent Runtime Decisions.md`; platform summary: `docs/decisions/Platform, Sessions, and Sandbox Decisions.md`; archived source notes: `docs/archive/decisions/Coder Delegation Collapse — Component Audit.md`, `docs/archive/decisions/Main as Scratchpad — Branch on Graduation.md`, `docs/archive/decisions/Scratchpad Durable Storage — Remote vs Phone-Local.md`. **PROTECTED throughout:** durable job engine, event-shape compat (`subagent.*`/`task_graph.*` + drift pins), capability gating, Protect Main, git-checkout/switch blocks, progress/liveness | You talk on `main` without branching first; the first commit auto-creates + auto-pushes a branch with no prompt, behind a secret scan that blocks on a hit; a *headless* detached **web/cloud** engine run produces identical branch behavior (nothing to prompt — CLI/daemon auto-branch is decided against per step (2), so this clause is web/cloud-only); the lead agent completes an interactive edit with no Orchestrator/Planner handoff and measures ≥ the delegated path before the Planner/brief is deleted; all PROTECTED categories unchanged (drift tests green, no advertised-but-denied tools, Protect Main + capability gates intact) |
| **Session Continuity & Stability (TUI ↔ Mobile)** — *active north-star (was first priority)* | in_progress | Make a session a durable, portable, authenticated thing so it can be started in one surface and continued in another. North star: start a session in the `push` TUI, pick it up on the phone over the relay (and the reverse), with no manual state surgery. Tracks: **(a)** legibility — relay failures surface the real reason instead of "1006" (shipped, #715); **(b)** **Universal Session Bearer** — every session has an attach token from birth + every attach requires it, retiring the tokenless class (**shipped 2026-05-30**, PRs #717 → #720 → #719): the `createSessionState` factory mints at birth, bootstrap-grace claims legacy sessions on first attach, the `!entry.attachToken` bypass is removed with `openAttach`/`PUSHD_OPEN_ATTACH=1` as the only opt-out, and the mint-on-demand patch is retired to a tripwire — verified end-to-end against the live daemon; **(c)** session stability — reconnect/resume robustness across surface handoff, building on Resumable Sessions + coder checkpoints; **(d)** deterministic resume — evaluate the opencode parts-model borrow if/when handoff exposes reconstruct-from-events gaps. See `docs/decisions/Platform, Sessions, and Sandbox Decisions.md`; archived source notes: `docs/archive/decisions/Universal Session Bearer.md`, `docs/archive/decisions/opencode SDK Review.md`. Relay desktop-wrapper polish (Phase 4) is downstream of this, not a substitute | A session created in the TUI is listed and resumable from the phone (and vice versa) and continues mid-conversation without losing context or requiring re-pairing; attach is uniformly authenticated (no tokenless sessions, no open-attach by accident); a failed relay connection states *why* in `relay status` rather than a generic close code; handoff survives a reconnect on either surface |
| **Background Exec Adoption — `sandbox_exec` Detached Path** — *promoted 2026-06-09* | done (shipped #863, 2026-06-09; live-tail UI stretch shipped #867, 2026-06-10) | Route the cloud `sandbox_exec` tool through the detached background-exec engine shipped in #789 (`exec-start`/`-status`/`-logs`/`-kill` routes + `lib/detached-exec-runner.ts`), replacing the buffered call whose ~165s client deadline makes long test/build runs impossible on web — every attempt times out, and (pre-#861) the resulting stamp-less stretch let the idle reaper hibernate the container mid-round. Scope: **(1)** adaptive poll schedule in the runner so short commands stay snappy (fast first polls, backing off to 1.5s); **(2)** abort-signal wiring — Stop interrupts the detached process and synthesizes the exit-124 envelope (parity with the local-pc path); **(3)** tail-capped output accumulation in the runner (long runs can't grow unbounded; `truncated` flag preserved); **(4)** structured logs on path selection + fallback (existing `background_exec_fallback`). Buffered exec remains the fallback for backends without the routes (Modal 404s the start call). Live-tail UI from `onProgress` is a stretch goal, not acceptance. Decision: `docs/decisions/Platform, Sessions, and Sandbox Decisions.md` §6; source note: `docs/archive/decisions/Background Execution — Detached Process and Resumable Cursor Logs.md` | A test suite that outruns the buffered ~165s deadline, run via `sandbox_exec` on the CF backend, completes and returns its real exit code with tail output intact; user Stop interrupts the detached process mid-run (exit-124 envelope, process actually killed); short commands stay within ~1s of buffered-path latency; Modal and local-pc behavior unchanged; no tool-schema change (no protocol drift surface) |
| Push CLI Muscle-Memory UX | in_progress | Continue aligning `push` with Claude Code/Codex-style terminal muscle memory. The TUI default now lives in `cli/cli.ts` rather than the launcher (opt out with `PUSH_TUI_ENABLED=0`), so direct `node cli/cli.ts` invocations match `./push`. Transcript-first REPL flows remain a first-class alternative for users who prefer them | Session flow feels transcript-first (prompting, interrupts, outputs, tool visibility), common tasks require fewer commands/flags, and day-to-day usage no longer depends on roadmap-specific operator knowledge |
| Remote Sessions Phase 4 — Desktop Wrapper Polish | planned | Polish the non-core packaging layer after Worker relay sessions shipped end-to-end: tray/menu-bar wrapper, launch-at-login ergonomics, and pairing UI around the existing `pushd` runtime | Desktop packaging improves startup/pairing ergonomics without becoming a separate runtime; Remote chat-tool dispatch continues to use the existing daemon protocol |
| Selective CLI Adoption of Shared Runtime | in_progress | Bring more of the now-shared runtime substrate into CLI only where it improves the terminal product (task framing, events, memory, later task-graph/runtime features) | Adopted CLI flows use the same runtime semantics as web without introducing new CLI-only semantic drift; any remaining differences are shell-specific by design |
| CLI/TUI-lite Ergonomics | in_progress | Add terminal UX improvements that stop short of a full-screen TUI (session picker, transcript navigation, command shortcuts, compact status surfaces). **Session hygiene shipped 2026-06-12:** `push sessions prune` (selector-based retention, dry-run by default, run-marker guard) + leak-proof test home isolation — the suite had leaked ~1,700 fixture sessions into `~/.push/sessions` (purged: 2,157 → 474, 68 MB → 16 MB) — and the TUI resume picker now defaults to the active workspace with an `a` toggle + hidden-count tag (#906/#908) | Interactive users can navigate session history and active runs faster without leaving the transcript-first model; the resume picker shows the current project's sessions first; the session store has an explicit retention tool and tests can no longer pollute it |
| Chat Surface Evolution | planned | Make chat a first-class surface with cleaner chat-first launcher framing, explicit context escalation, and mode-specific restore behavior | Chat can be described as its own surface instead of a workspace flag, while runtime/storage/auth remain shared |
| Workspace Publish Follow-through | planned | Polish the post-publish repo-backed handoff and decide the optional empty-repo path after the first publish flow shipped | Publishing a workspace to GitHub feels explicit and understandable end-to-end, and the next empty-repo path is either shipped or clearly scoped |
| Workspace Hub v2 | planned | Improve Diff ergonomics and decide long-term drawer vs hub division for history/settings | Decision captured; richer per-file diff navigation shipped; no duplicate navigation paths |
| Autonomous Webhook PR Review | done (v1 + reliability arc; **first structured autonomous review verified live 2026-06-12**) | GitHub App webhook → `PrReviewJob` Durable Object runs an agentic, cancellable PR review and posts it back, with REVIEW.md-by-default guidance and a PWA review-history surface. v1: receiver + DO (#690), shared token-injectable `github-tools` client (#691), PWA history (#692), web-tool suppression (#694), deep + cancellable reviews (#693), manual re-run (#695), opt-in Checks-API gating. **Reliability arc (2026-06-10/12):** auto-retry on death paths (#866) → relaunch-from-checkpoint + watchdog (#888) → degraded-honesty (#907: narration refused on every fallback exit, degraded reviews unposted, check-run reads neutral "Review incomplete" instead of a fake pass) → convergence (#909: in-loop wrap-up pressure, post-loop checkpoint so forced-turn deaths resume at the forced turn, 12-round budget, reasoning counts as activity) → reviewer model swapped zen/glm-5.1 → blackbox/grok-code-fast-1:free (glm: 0 structured reviews since #810; grok: 8/12 rounds, 3 real findings, no relaunches) → sakana/fugu (Blackbox removed as a provider 2026-06-27 for chronic flakiness; built-in default reviewer is now Sakana Fugu) → config pinned per delivery at start (#910: mid-flight settings swaps can't kill running reviews; row reservation kept synchronous against redelivery/coalesce races). See `docs/decisions/Platform, Sessions, and Sandbox Decisions.md` | Opening/pushing a PR triggers an advisory review posted to that PR; an incomplete run reports honestly (neutral check, nothing posted) instead of a green no-findings pass; runs survive DO deaths via checkpoint relaunch; deliveries deduped/coalesced per head SHA; config changes never retro-apply to in-flight reviews |
| **Lossless Context Management (LCM) follow-through** | in_progress | Phases 0–2 shipped (PRs #749/#750/#751): verbatim retrieval kernel, packer `detail` opt-in + `[mem_…]` ids, Auditor opt-in (web + CLI), and model-facing `memory_grep`/`memory_expand`. Part **(a) — web Coder + Deep-Reviewer routing — DONE 2026-06-01:** `buildCoderToolExec` now accepts a `memory` source via an injected scope-bound `executeMemory`; the **delegated** web Coder (scope threaded through the delegation envelope) and the Deep-Reviewer (executor already supported it) advertise the tools, gated to match executor support. The **background coder-job** (Worker/DO) is deferred — its `getDefaultMemoryStore()` isolate starts empty (no Worker-side persistent store), so memory there is intentionally unwired. Remaining: **(a′)** a Worker-side memory store so background jobs can recall; **(b)** Phase 3 — an optional append-only verbatim log for true losslessness. See `docs/decisions/Agent Runtime Decisions.md`; source note archived in `docs/archive/decisions/Lossless Verbatim Memory Retrieval (LCM).md` | ✅ Web Coder and web Deep-Reviewer can call `memory_grep`/`memory_expand` (executors route the `memory` source, scope injected from session context, never model args); advertising matches executor support on every surface (no advertised-but-denied tools) |
| **Auth Rework — GitHub as the Single Identity Anchor** | done (2026-06-04) | Collapsed three mismatched auth mechanisms onto one anchor: a GitHub-identity session is the universal `/api/*` gate, replacing the retired `X-Push-Deployment-Token`. **(1)** session minted at GitHub App-OAuth + installation-id time, verified per request against `GITHUB_ALLOWED_USER_IDS`, ran observe → enforcing in prod (#776/#777). **(2)** per-repo App-coverage detection (`/api/github/repo-coverage`) with an actionable install/update prompt instead of a cryptic clone failure; `needs_ack` reframed legacy-only; no PAT UI (#778). **(3)** widened the gate to universal (#780), then removed the deployment token (`deployment-auth.ts` + middleware check + `#push_token`), replaced `DeploymentTokenGate` with `GitHubSignInGate`, repurposed `/api/auth-probe` as the session probe. Secrets: `PUSH_SESSION_SECRET` + `GITHUB_ALLOWED_USER_IDS` + `PUSH_SESSION_GATE_ENFORCE`. Decision doc now `Current`. Operator deletes the `PUSH_DEPLOYMENT_TOKEN` secret post-deploy | The metered surface is gated on an allowlisted GitHub identity end-to-end (verified in prod); no `/api/*` route is reachable without a session except bootstrap/self-auth endpoints; the deployment token and its client gate are gone; decision doc flipped to `Current` |
| TUI Decomposition (testability seam → daemon-session controller) | done (Phases 0–1; Phase 2 optional, reassess only if `tui.ts` stays unwieldy) | Decompose the `runTUI` closure, net-first. **Phase 1 shipped 2026-07-09 (#1369):** `cli/tui-daemon-session.ts` is the `DaemonSessionController` — it privately owns the client/sessionId/attachToken trio plus reconnect backoff, autostart guard, build stamp, and the event-seq cursor; `runTUI` holds one `createDaemonSession({ …hooks })` and the ~100 ambient reads collapsed to `daemon.*`; verbs are typed methods (`revert`/`unrevert`/`summarize`/`listChildren`/`getChild`); the resume-modal session ops route through `options.deps` (Phase 0's flagged grafting points). Verified against the unchanged #740 characterization suite plus new disconnect→backoff-reconnect coverage (`cli/tests/tui-daemon-session-controller.test.mjs`). **Phase 0 shipped:** an `options.io` / `options.deps` seam (process/IO + daemon-client factory + config/session loaders) makes `runTUI` drivable headlessly, plus a `cli/tests/tui-driver.mjs` harness and characterization tests pinning the #740 session verbs (`/revert`→`session_revert`, `/unrevert`, `/children`→`list_children`, `/compact`→`session_summarize`, `NOTHING_TO_UNREVERT`→status-not-error) via the real input→dispatch→send path — retroactively closing the test gap punted in #740. Remaining: **Phase 1** — extract a `DaemonSessionController` owning the daemon-session state (~90 reads / 12 assignments) + verb methods (medium-high risk; gated on the Session Continuity track settling since it's actively adding inline daemon state); **Phase 2** (optional) — a command-handler module. See `docs/decisions/Agent Runtime Decisions.md`; source note archived in `docs/archive/decisions/TUI Decomposition - Testability Seam and Daemon Session Controller.md` | The TUI's session verbs are covered by headless tests exercising the real dispatch path; the daemon-session lifecycle has one owning module; no per-surface semantic drift introduced |

## Recently Completed

| Item | Status | Scope | Acceptance Criteria |
|---|---|---|---|
| **Auto-Branch on Commit (web/cloud)** — *first-priority step (2)* | done (2026-06-13) | Committing while HEAD is on the default branch forks to a new branch first, so nothing lands on `main`; the chat follows onto it (`kind:'forked'`). One shared `ensureCommitTargetBranch` seam (#921 doc → #922 impl): an injectable fork primitive (bare `forkBranchInWorkspace` for the model path / file browser; `forkBranchFromUI` for the hub), model-proposed branch name with a *total* deterministic fallback, collision-suffix loop, flag `PUSH_AUTO_BRANCH_ON_COMMIT` (default-on). Wired into the model tool path (`handlePrepareCommit`, branch awareness threaded through the exec-options chain) + the web commit surfaces; **the workspace hub's previously-raw `git commit`/`git push` migrated to the gated `createSandboxPushGit` path**, closing a pre-existing secret-scan gap. `MergeFlowSheet` is not a commit surface (GitHub-API merges only) — no hook. CLI deferred. Doc → Current. *(Diagnosed + fixed a same-day prod fallout: a missing-path `list`/`read` was misclassified as a gone sandbox and killed inline turns — #923/#924, sandbox file-not-found vs sandbox-gone classification, found via `wrangler tail`.)* | Committing on the default branch lands on an auto-named branch and pushes there through the gated path; a commit already off-default is untouched; flag-off reverts to prior behavior; the chat migrates onto the new branch; model-Auditor + Protect Main unchanged |
| **Branch Transition Governance — Warm Switching, Desync Detection, Branch Moments** | done (2026-06-12/13) | Routed every branch transition through one governed typed-path dispatcher (`applyBranchSwitchPayload`) — the substrate `auto-branch-on-commit` (first-priority step 2) builds on. **Warm UI switching** (#911 doc → #912): UI branch switches go through `sandbox_switch_branch` when a sandbox is live (warm, sandbox preserved) with a state-aware dirty-tree dialog; teardown demoted to backstop; first `skipBranchTeardownRef` test coverage. **Branch-desync detection** (#913): every `sandbox_exec` carries a best-effort branch stamp (`git symbolic-ref`, so unborn/orphan branches stamp too); the web compares at both lead seams (orchestrator dispatch + inline kernel tee) and either reconciles toward sandbox HEAD through the governed path or surfaces detached HEAD; `branch_desync` event + drift pins. **Branch Moments** (#914 doc → #915–#917): moment-anchored transition producers — commit-card switch/fork chips, an out-of-band-merge banner (reuses the in-app merge migration; `merge_detected` source), and a `carry_chat` verb (`kind: 'carried'` + divider) so the model can continue a chat on another branch. The carry verb closed a verified hole: the inline lane structurally dropped `branchSwitch` payloads (no field on `SandboxToolExecResult`), so model-initiated switches never routed chats in the default mode until the #913 tee pattern was extended. Decision docs archived to `docs/archive/decisions/` (#920). | A UI branch switch preserves the live sandbox; sandbox HEAD moved by raw git (rebase/bisect/scripts) is detected and the UI follows or honestly surfaces detached HEAD; the model can switch-and-carry a conversation onto another branch; all transitions share one dispatcher |
| Lossless Verbatim Memory Retrieval (LCM) — Phases 0–2 | done | Started from an r/LangChain post on the Voltropy LCM paper (arXiv 2605.04050); the technique is real, the wild `pip install openlcm` package was unverified and explicitly not adopted. **Phase 0** (#749): deterministic `lib/context-memory-expand.ts` kernel (`expandMemoryRecords`/`grepMemory`, substring-not-regex). **Phase 1** (#749): packer surfaces top-ranked verbatim `detail` behind `MemoryPackOptions.includeTopDetail` + leads records with `[mem_…]` ids. Auditor opt-in (#750) on both surfaces via shared `AUDITOR_MEMORY_PACK_OVERRIDES`. **Phase 2** (#751): model-facing `memory_grep`/`memory_expand` — new `memory` tool source + `memory:read` capability (all roles), shared `lib/memory-tool-exec.ts` with scope injected from session context (never model args), symmetric structured logs. Advertised where the executor supports it: web Orchestrator/Explorer + CLI Orchestrator/Coder/Explorer/Deep-Reviewer. See `docs/archive/decisions/Lossless Verbatim Memory Retrieval (LCM).md` | A model can recall the exact prior text of a decision/finding/verification record mid-turn via `memory_grep`→`memory_expand`, scoped to its repo/branch; the Auditor sees verbatim verification output in its SAFE/UNSAFE context; no advertised-but-denied tools on covered surfaces |
| Remote Sessions via pushd Relay — Phases 1-3 | done | Local PC and Remote daemon sessions now route real chat turns through `pushd`. Phase 1 shipped the loopback WS, pairing UI, `LocalPcChatScreen`, `sandbox_exec`, file read/write/list, `sandbox_diff`, cancellation, reconnect, and approval UI. Phase 2 shipped the Worker/Durable Object relay path, `push daemon pair --remote`, phone attach/replay, token-hash allowlist hardening, long-lived WS reuse for chat dispatch, and the shared `DaemonChatBody` shell. Phase 3 shipped repo allowlist, connected-device management, attach-token cascade, audit log, and approval-decision events | A paired Local PC or Remote session can run `sandbox_exec`, `sandbox_read_file`, `sandbox_write_file`, `sandbox_list_dir`, and `sandbox_diff` through the daemon from the chat surface; cloud sessions are unchanged; remaining work is Phase 4 desktop-wrapper polish |
| Renderable Artifacts (web v1) | done | Model-emitted `create_artifact` tool calls now persist to a Workers KV namespace and render inline in chat as kind-dispatched cards: Sandpack-backed static-html / static-react previews (with an Expand-to-modal affordance), Mermaid diagrams (with strict security level + dynamic import), file-tree viewers, and a coming-soon stub for live-preview. Shipped across #487 (`lib/artifacts` scaffold + handler + store), #488 (CLI executor wiring), #489 (Coder grant on CLI), #490 (`/api/artifacts/*` worker routes + `WebKvArtifactStore`), #491 (web orchestrator dispatch + 5 lazy-loaded renderers + dispatcher entry), and `ae1eae1b` (binding the production `ARTIFACTS` KV namespace). Web artifacts are chat-scoped (`repoFullName + branch + chatId`); CLI files under `repoFullName + branch`. The runtime refuses to persist a web artifact without a chat id (non-retryable `INVALID_ARG` / `MISSING_CHAT_ID`) so misconfigured callers surface immediately rather than silently filing under the wider scope | A `create_artifact` from chat round-trips through the worker, persists to KV, and renders the right kind-specific card; per-kind renderers code-split (Mermaid chats don't pull Sandpack and vice versa); the CLI `create_artifact` path is unchanged and continues to file under `~/.push/artifacts/<scope>/<id>.json`; live-preview is intentionally a stub pending sandbox-side dev-server orchestration |
| UX: Preserving Context on Branch Creation | done | "New Branch from Here" UI flow plus `create_branch` (alias `sandbox_create_branch`) and `switch_branch` (alias `sandbox_switch_branch`) tools. *Forking* (creating a new branch from current state) migrates the active conversation onto the new branch; *switching* (to an existing branch) routes to that branch's existing chat or auto-creates one if none exists. Sandbox stays alive across typed branch-tool transitions. Foreground tools emit `branchSwitch` for UI routing; background coder jobs emit `meta` for observability only. Shipped as slice 2 (#412), slice 2.1 (#413), slice 2.5 (#414), and slice 3 (#415) | A new branch can be created from an active chat without dropping that conversation, an existing branch can be switched to without destroying uncommitted work while restoring the target branch's chat context, and raw `git checkout` / `git switch` is blocked in `sandbox_exec`: any single bare operand blocks, including path-shaped `checkout` operands such as `src/utils.ts`, while explicit file restores use `git checkout -- <path>` or `git checkout HEAD <path>` |
| PushStream Gateway Migration | done | Migrated model-streaming transport from the 12-arg `ProviderStreamFn` callback contract onto the async-iterable `PushStream` contract end-to-end across web AND CLI. Provider side: all eleven providers (Ollama Cloud + Cloudflare Workers AI + OpenRouter + OpenCode Zen + Kilo Code + OpenAdapter + Nvidia NIM + Blackbox AI + Azure OpenAI + AWS Bedrock + Google Vertex) ported onto native `<provider>Stream` modules consuming the shared `lib/openai-sse-pump.ts`. Consumer side: every agent role (Auditor, Coder, Reviewer, Planner, Explorer, DeepReviewer) iterates `PushStream` events directly via `iteratePushStreamText`. CLI side: `cli/provider.ts#streamCompletion` ported onto the same gateway via a new `cli/openai-stream.ts` (#402); the two queue/notify PushStream shims that bridged `streamCompletion`'s callback API into a `PushStream` for lib-side agent roles (`createDaemonProviderStream`, `buildPlannerPushStream`) deleted in favour of the native PushStream (#403). Both bridges (`providerStreamFnToPushStream`, `createProviderStreamAdapter`), the `ProviderStreamFn` callback shape, `legacyChatPushStream`, `PROVIDER_STREAM_CONFIGS`, the `streamSSEChat` / `streamSSEChatOnce` machinery, and the CLI shims are all gone. See `docs/archive/decisions/PushStream Gateway Migration.md` for the phase-by-phase log | All agent roles + the CLI consume `PushStream` events directly; both bridges, the legacy callback shape, the legacy SSE path, and the CLI shims are gone; web and CLI share `lib/openai-sse-pump.ts` end-to-end |
| GitHub Tools: Fetch inline PR Review Comments | done | `pr` now fetches inline `pull_request_review_comments` plus top-level issue comments, and the shared/web renderers expose both alongside the diff | Agent can read reviewer feedback on a PR without the user copy-pasting it; shared PR tool tests and PR card rendering cover both comment classes |
| Sandbox Awareness Matrix | done | Session capability blocks now include container TTL, remaining TTL, persisted lifecycle events, readiness hints, and workspace lifecycle history directly in the prompt context | Models receive rich lifetime and event history in prompt context and can proactively reason about save/download timing without an external dashboard |
| `pushd` Attach + Event Stream UX | done | Shipped explicit CLI attach/resume event streaming over the existing NDJSON socket protocol, including raw v2 delegation events, local attach-token payloads, visible subagent transcript boundaries, and transcript-compatible task-graph node-focus snapshots | User can attach to a live session, watch delegation progress in a readable transcript, and recover after disconnect without manual state inspection; focused formatter/handler tests, CLI typecheck, and daemon integration coverage are green |
| CLI Protocol/Schema Hardening | done | Hardened the pushd wire envelopes with a runtime schema validator (`cli/protocol-schema.ts`) covering the `SessionEvent` contract plus per-type payload schemas for the nine delegation events (`subagent.*` × 3, `task_graph.*` × 6). Explicit rejection of `runId: null` and `payload: undefined`. Strict-mode validation wired into `broadcastEvent()` under `PUSH_PROTOCOL_STRICT=1`, enabled in the daemon-integration test harness so every handler test validates its outgoing events | Schema validation mode exists and is on for core events in `test:cli`; drift guard-rail tests re-extract delegation event literals, `RunEventSubagent`, and `TaskGraphNode.agent` from `lib/runtime-contract.ts` at test time so a new variant without a matching validator fails the suite; 56 new unit + integration tests shipped |
| UX: Multi-image Upload | done | Verified web chat UI already supports batch selection/upload and that orchestrator + provider adapters (OpenAI, Anthropic, OpenRouter) pass every staged image through as a distinct vision block | User can upload multiple screenshots per turn and the agent processes them in a single vision-block payload; no single-image bottleneck in the serialization path |
| Shared Runtime Convergence Tranche | done | Shipped the main semantic web/CLI convergence pass: shared runtime contract, task graph executor, typed context-memory stack, delegation brief formatter, run-event vocabulary, role-context helpers, and first CLI adoption of shared task framing/events | Core runtime semantics live in shared `lib/`; web uses the canonical modules; CLI has started consuming the same contracts without forcing blanket feature parity |
| Task Graph Orchestration + Typed Context Memory Foundation | done | Shipped dependency-aware `plan_tasks`, task-level graph events, graph memory, typed memory retrieval, invalidation, and sectioned prompt packing | Orchestrator can execute mixed Explorer/Coder task graphs with dependency context; typed memory retrieval and freshness-aware packing are live in the main runtime |
| Workspace Publish to GitHub Phase 1 | done | Added first-class in-app publish UI for scratch workspaces plus launcher entry points and workspace transition plumbing | User can publish a scratch workspace to a private/public GitHub repo from the app without relying on a chat-only tool flow |
| Harness Reliability Program (Tracks A-C + E) | done | Shipped the harness-first reliability push: hashline edits, range reads + truncation-aware safety, garbled tool-call recovery, structured error taxonomy, multi-tool dispatch, meta envelopes, acceptance criteria, working memory, symbol reads, and patchsets | Core checklist shipped; harness reliability remains an ongoing product priority, with future work handled as incremental maintenance rather than a roadmap checklist |
| Push CLI Foundation + Harness Transition (No Checkpoint Resume) | done | Built modular `push`/`pushd` foundation and added advanced harness semantics to CLI runtime: multi-tool turns (parallel reads + single mutation), hashline `edit_file`, working memory updates, malformed tool-call diagnostics, file-ledger/meta envelopes, and headless acceptance checks | `push` supports config init/show/set and persisted defaults; `run --json`/`sessions --json` stable; protocol envelope persisted in session events; advanced harness features shipped in `9ec31c5` with passing `scripts/push/tests/*.test.mjs` |
| Resumable Sessions (Phases 1-4) | done | Added interruption-safe run checkpointing + resume flow across orchestrator and coder delegation, including reconciliation and lock safety | Interrupted runs surface a `ResumeBanner`; resume revalidates sandbox/branch/repo, injects `[SESSION_RESUMED]` with sandbox HEAD/dirty/diff context, preserves coder state, and records resume telemetry |
| Agent Experience Wishlist (Track C) | done | Implement 10 harness improvements: structured error taxonomy, edit diffs, multi-tool per turn, universal meta envelope, and machine-readable tool-call feedback | All items shipped and verified in harness metrics (Commit 0336f11) |
| Scratch Workspace v1 (formerly Sandbox Mode) | done | Ephemeral Modal workspace for brainstorming/prototyping; primary onboarding entry point; tar.gz download export path | User can start a scratch workspace from onboarding (no GitHub auth) or repo picker, edit/run files, and download workspace as tar.gz |
| Repo Sync Reliability | done | Unified auth handling and complete repo pagination for PAT/OAuth + GitHub App paths | Authenticated flows do not silently fall back to demo; repo fetching paginates across accessible pages |
| Scratch Workspace Repo Promotion (v2) | done | In-app `promote_to_github` flow creates a repo, pushes the scratch workspace branch, and transitions app context to repo mode | User can promote a scratch workspace to GitHub without leaving chat; active workspace rebinds to the promoted repo |
| Workspace Hub v1 | done | Single top-right workspace trigger with full-screen hub tabs (`Files`, `Diff`, `Console`, `Scratchpad`) and integrated commit/push controls | Hub opens reliably on mobile; tab flows work; commit/push confirmations and protect-main guard enforced |
| Branch UX Consolidation (Phase 1) | done | Branch selector shows existing branches (not just app-created), includes delete action in selector, and home cards support branch selection | User can open repos on specific branches from Home and manage branch switching/deletion from workspace context |
| Home Header Simplification | done | Replaced ambiguous top header strip with compact account menu (settings/disconnect) and profile avatar | Home uses less vertical space and account controls remain discoverable |
| Sandbox Read Efficiency (Track B Phase 1) | done | Added `sandbox_read_file` line-range args, range line numbering for tool output, and explicit warning for out-of-bounds empty ranges | Agent can request targeted ranges and gets clear warning signal when range is beyond file content; editor card content remains clean |

## Decision Log

| Date | Decision | Source |
|---|---|---|
| 2026-06-15 | **First-priority step (1) remainder shipped; web Orchestrator-loop retirement is now the next step-(1) frontier.** Planner/brief deleted (#940), CLI engine loop's duplicated round machinery retired (#942/#943 — every CLI turn runs the shared kernel via `runLeadKernelTurn`), and the last gate on web Orchestrator-loop retirement — multi-image-history fidelity — closed: #937 carried current-turn attachments onto the inline + background lanes, #938 carried *prior-turn* attachments, finishing with the background DO sourcing them from the same job-chain walk as the summaries (one source, no client/server drift) after a Codex review pass (shared merge helper, `visibleToModel` filter, `/turn-summary` "no summary, no attachments" contract). The delegated org-chart round loop (`toLLMMessages` in `app/src/lib/orchestrator.ts`) is now the only path the inline lead doesn't fully subsume — unblocked, not started. | PRs/commits #936–#943, #938 arc (`0833bc26`→`15a6b6b1`); CLAUDE.md §10 pointer |
| 2026-06-14 | **CLI/daemon auto-branch-on-commit decided *against* (drops it from first-priority step 2's remainder).** Auto-branch is a durability mechanism — it gives committed work a remote home because the cloud sandbox is ephemeral and `main` is a never-pushed scratch floor (the Anthropic-cloud counterexample branches-before-first-response *because* its persistence is git-with-no-scratchpad-floor). On the CLI the persistence story inverts: the local git repo on disk *is* the durable home, so the load-bearing reason is absent. The `ensureCommitTargetBranch` seam (`app/src/lib/`) is `sandboxId`/`execInSandbox`-shaped to its core — web code whose *premise* is the sandbox, not code that needs porting. What remains of "don't clobber `main`" is already covered locally by the shared Protect Main pre-hook (`createProtectMainPreHook`, registered `cli/tool-hooks-default.ts:35`); reachability rides the pushd relay, not a pushed branch; and auto-forking local commits breaks Claude Code/Codex muscle-memory parity (a stated CLI priority). Revises the 2026-06-03 "flag the substrate, not the commit-flow" note — "keep it universal for consistency" once the load-bearing reason is gone is the ceremony the single-agent-loop track exists to cut. The pre-push secret scan (orthogonal to branching) stays on any pushing CLI surface. | Chat decision; `lib/git/policy.ts`, `cli/tool-hooks-default.ts`, `app/src/lib/ensure-commit-target-branch.ts` |
| 2026-06-13 | **Auto-branch-on-commit shipped for web/cloud (first-priority step 2).** Doc #921 → impl #922 (Codex foundation, Claude finish), scoped doc-first because it's higher-blast-radius than the branch-moments slices (push transport + the "nothing lands on main" invariant). Owner calls: model-proposed name with a total deterministic fallback, web/cloud only, flag default-on with a kill-switch. The doc-first cadence paid off — Codex caught the gate-2 hub secret-scan gap at *spec* review and a git-rejected-name fatal at *code* review. Same-day prod fallout (a missing-path `list` misclassified as a gone sandbox → killed inline turns) diagnosed via `wrangler tail` and fixed across #923/#924 (file-not-found vs sandbox-gone classification, both directions). Remaining first-priority steps: (1) Planner/brief deletion remainder, (3) Auditor unbundle (secret-scan done; model-Auditor KEPT per the 2026-06-08 reversal), (4) platform storage substrate. | `docs/decisions/Auto-Branch on Commit — Nothing Lands on Main.md`; PRs #921–#924 |
| 2026-06-13 | **Branch-transition governance trilogy shipped; the UI and the model now share the typed path.** Warm UI switching (#911/#912), branch-desync detection (#913), and Branch Moments (#914–#917) landed in sequence: UI switches are warm and governed, raw-git HEAD movement (rebase/bisect/scripts) is detected and reconciled, and the model gained a `carry_chat` verb. Sequencing was load-bearing — shipping warm-switching first converted the dominant legitimate source of "teardown as consistency guard" into governed traffic, so detection landed over a system whose *intended* transitions all flow through `applyBranchSwitchPayload`. This is the branch-governance substrate `auto-branch-on-commit` (first-priority step 2) builds on. Open thread recorded in the Branch Moments doc: whether a *delegated* run's HEAD movement should reconcile the foreground UI mid-delegation (tee callback deliberately undefined there). Same-session housekeeping: remote+local branches pruned to `main`, the four shipped per-feature decision docs archived (#920), CLAUDE.md's tool-protocol note updated for the reasoning-channel recovery nudge (#918/#919). | PRs #911–#920; `docs/archive/decisions/{Warm Branch Switching — The UI Joins the Typed Path, Branch Desync Detection, Branch Moments — Transitions Where Intent Lives}.md` |
| 2026-06-10 | **Durable Runs — Adopt-on-Silence promoted to first priority.** The run must survive the phone: browser-resident loop + mobile tab eviction undermines the mobile-first premise, and the Background Exec track made the asymmetry sharp (commands outlive the tab, the agent turn awaiting them does not). Chosen shape preserves the current attached-client responsiveness wholesale — local while watched, RunHost-DO adoption from per-turn checkpoints when heartbeats lapse, bearer-authenticated attach on return. Phase 0 gates the build with instruments: a DO-relay latency spike (decide on numbers, not vibes) and the agent eval harness the delegation-collapse A/B was silently missing. Single-Agent Loop step 1 (delegation collapse) folds in as the Phase 2 precondition — simplify the loop as it goes host-agnostic rather than durable-izing the Planner/brief scaffolding and re-cutting it after. Steps 2–4 of that track unchanged. | `docs/decisions/Durable Runs — Adopt-on-Silence.md` |
| 2026-06-09 | **Background Exec Adoption shipped (#863) — cloud `sandbox_exec` runs detached.** The buffered ~165s ceiling is gone: long test/build runs complete under a 10-min runner deadline, Stop interrupts the detached process server-side (exit-124 cancel envelope, parity with local-pc), and every status/log poll stamps idle accounting so long runs can't race the hibernation reaper (#861). Runner upgrades: adaptive poll schedule (`[250,500,1000,1500]`ms ramp), `abortSignal`, 256k/stream tail-capped accumulation, bounded terminal drains. Two Codex review rounds added `terminalReason` provenance (`completed`/`cancelled`/`deadline`/`lost-contact`/`start-unconfirmed`) on every runner return path, a double-execution guard (buffered fallback only on definitive 404 — ambiguous start failures are deliberately not retried), honest outcome-unknown envelopes for post-start `-1`, and conservative cache/ledger invalidation when a mutating command may have run (lost-contact AND mid-run cancel). Same-day arc: #861 (idle-reaper liveness) → #862 (promotion) → #863 (adoption). Stretch shipped next-day (#867): live output tail in the agent status bar via `onProgress` → `exec-progress.ts` (throttled last-line, CR-aware for progress bars, ANSI-stripped, log-excluded + abort-gated per Codex review). | PRs #861, #862, #863, #867; `docs/decisions/Platform, Sessions, and Sandbox Decisions.md` §6 |
| 2026-06-06 | **Deterministic pre-push secret scan shipped — the first concrete piece of the Auditor unbundle (first-priority step 3) and the keystone `auto-branch-on-commit`'s auto-push will reuse.** Built the model-free scanner kernel (`lib/secret-scan.ts`: high-precision provider-token/private-key/cloud-credential rules, precision-over-recall since there's no human at auto-push) + a `PrePushGate` seam on `PushGit.push()` (`lib/git/push-git.ts`) built by `lib/git/secret-scan-gate.ts`, wired into the web commit/push flow (`useCommitPush.ts`) over the in-hand diff. Match → fail closed (block); infra trouble → fail open + loud structured log (deliberately not the model-Auditor's "flaky backend blocks the op" liability). Opt-out `PUSH_SECRET_SCAN=0`. Chosen as the next slice because it has zero gating dependency (unlike the step-2 Planner cut, which needs A/B data, and the storage-substrate flag, which needs the unresolved identity/continuity tradeoffs). Remaining: route the model `push` tool / `promote` / card-action pushes through the gate via a git-computed pushed-diff (deferred to the auto-push slice; model commits stay Auditor-gated until then). | `docs/archive/decisions/Main as Scratchpad — Branch on Graduation.md` (open-Q #2), `docs/archive/decisions/Coder Delegation Collapse — Component Audit.md` (Category 4) |
| 2026-06-03 | **Single-Agent Loop + Branch-at-Commit promoted to first priority.** Frontier-lead makes the weak-model / git-only scaffolding overhead. Collapse the Coder-delegation wrapper (lead drives the durable job engine directly — keep the engine, cut the Planner/brief) and make the branch automatic at the first commit (`auto-branch-on-commit` + auto-push behind a deterministic secret scan; `main` is a best-effort snapshot scratchpad, no commits land on it). The model-Auditor *unbundles* — function protected, implementation relocated to the secret-scan + existing PR reviewers + Protect Main. Storage substrate is the *only* platform-flagged piece (APK local-git vs PWA remote-snapshot), gated on two conscious tradeoffs (cross-surface-continuity narrowing + identity pick). **Sequenced:** collapse delegation first (it has a 21-case test suite + a clean cut list), then layer auto-branch (its "who commits" answer is cleanest once the single-agent loop exists). Anchored on the Anthropic-cloud counterexample: that env branches before the first response + stop-hooks on uncommitted changes *because* its persistence layer is git with no scratchpad floor — Push's snapshot floor frees the branch to the commit. Demotes Session Continuity from first-priority to active north-star (still in_progress). | `docs/archive/decisions/Main as Scratchpad — Branch on Graduation.md`, `docs/archive/decisions/Coder Delegation Collapse — Component Audit.md`, `docs/archive/decisions/Scratchpad Durable Storage — Remote vs Phone-Local.md` |
| 2026-05-30 | **Addressable Session Verbs follow-up hardening shipped.** PR #729 closed the post-completion gaps from review: `submit_approval` is now bearer-gated before pending-approval lookup (matching the `cancel_run` auth posture), transcript-mutating session verbs (`session_summarize`, `session_revert`, `session_unrevert`) now trigger attached TUI/Remote clients to refetch `get_session_messages` instead of rendering stale local history, and the `docs/cli/schemas` request/event enums were partially reconciled with the live daemon vocabulary while still documented as non-runtime spec artifacts. | PR #729; `cli/pushd.ts`, `lib/session-transcript-events.ts`, `docs/cli/schemas/README.md` |
| 2026-05-30 | **Addressable Session Verbs track complete.** All phases merged (#722 doc → #723 cancel_run auth gap → #725 abort → #724 list/get children → #726 event-derived children → #727 session_summarize → #728 session_revert/unrevert). 17 bearer-enforced session verbs; doc flipped Draft → **Current**. Along the way: closed a real `cancel_run` auth gap the bearer's 11-site sweep missed (12th enforcement site); deferred the live `attach_child_session` fan-out as redundant (child events already broadcast to session-attached clients); scoped `session_revert` to transcript-only (last-N user turns + unrevert), leaving code/git rollback to the existing typed branch tools. Out of scope (per the doc): a session REST API (opencode #3), the parts model, and `share`/`shell`/`command` verbs. | `docs/archive/decisions/Addressable Session Verbs.md`, PRs #722–#728 |
| 2026-05-30 | **Addressable Session Verbs track started** (follow-on to the bearer; opencode review suggested-priority #2 graduated). Pins one canonical spelling for the session-lifecycle verbs Push does internally (`abort`/`revert`/`summarize`/`children`). Decisions: doc + thin executable aliases where daemon-reachable, hybrid naming (Push's shipped names stay canonical), children modeled as addressable child sessions. A machinery audit reversed the "thin alias" premise: only `abort` is thin — and it surfaced a real **`cancel_run` auth gap** (session-ful path aborts a run with no `validateAttachToken`; not among the bearer's 11 sites). `summarize` needs a `lib/` promotion; `revert` needs a real checkpoint/truncate build; `children` is ~80% there. Phased implementation sequence in the doc. | `docs/archive/decisions/Addressable Session Verbs.md`, `docs/archive/decisions/opencode SDK Review.md` |
| 2026-05-30 | **Universal Session Bearer shipped (track (b) of Session Continuity & Stability).** Merged as a three-PR stack: #717 (`createSessionState` factory mints the attach token at birth, all 3 creation points routed through it) → #720 (bootstrap-grace legacy claim in `handleAttachSession` + `attach_session` returns the token + TUI/CLI adopt-from-response; logs `legacy_claim`) → #719 (drop the `!entry.attachToken` bypass, add `openAttach`/`PUSHD_OPEN_ATTACH=1` opt-out with `open_attach_used`, retire `resolveOrMintTargetAttachToken` to a tripwire). The audit table (3 creation points, 11 enforcement sites, per-client token sources) was re-verified per PR; the stale-in-memory lockout is closed by grace + adopt shipping together. Verified end-to-end against the live `pushd` daemon (mint+enforce, grace claim across a daemon restart, opt-out). Doc flipped Draft → Current. Unblocks the addressable-session layer (bearer → session verbs → optional session API). | `docs/archive/decisions/Universal Session Bearer.md`, PRs #717, #720, #719 |
| 2026-05-30 | **Session Continuity & Stability promoted to first priority.** North star: start a session in the TUI and continue it on the phone (and vice versa) without manual state surgery. Consolidates the relay, session stability/resume, and the auth model into one theme. Track (a) relay legibility shipped (#715 — real rejection reason + fatal-no-retry instead of "1006"). Track (b) Universal Session Bearer design completed with both forks decided (Universal bearer enforcement + bootstrap-grace legacy cutover), grounded in a full attach-path audit; graduates from Draft to ROADMAP-tracked. Track (d) parts-model borrow stays deferred until handoff actually exposes a reconstruct-from-events gap. | `docs/archive/decisions/Universal Session Bearer.md`, `docs/archive/decisions/Remote Sessions via pushd Relay.md`, `docs/archive/decisions/opencode SDK Review.md`, PR #715 |
| 2026-05-13 | Remote Sessions via `pushd` reached the intended phone-surface milestone. Phase 1 chat-layer wiring and tool fan-out are shipped (`LocalPcChatScreen`, `localDaemonBindingRef`, `sandbox_exec`, `sandbox_read_file`, `sandbox_write_file`, `sandbox_list_dir`, `sandbox_diff`, cancel/reconnect/approval UI). Phase 2 Worker relay is shipped end-to-end (`push daemon pair --remote`, relay DO, phone attach/replay, hash/token-id allowlist hardening, long-lived WS reuse). Phase 3 permission/audit model is shipped. Remaining scope is Phase 4 desktop-wrapper polish, not core runtime. | `docs/archive/decisions/Remote Sessions via pushd Relay.md` |
| 2026-05-12 | Remote Sessions Phase 1 substrate + dispatch seam landed. Five PRs across two days: #507 (pushd-ws WS listener + device-token pairing + origin binding), #508 (`protocol-schema.ts` → `lib/`), #509 (web-side adapter with subprotocol bearer carry), #510 (flag-gated `VITE_LOCAL_PC_MODE` pairing UX + paired workspace shell + IndexedDB `paired_devices` store), #511 (dispatch seam: `SandboxExecutionOptions.localDaemonBinding`, `sandbox_exec` fork, `daemon_identify` round-trip). Superseded by the 2026-05-13 completion row above. | PRs #507, #508, #509, #510, #511 |
| 2026-05-07 | Renderable Artifacts shipped on web. Four-PR foundation (#487 substrate, #488 CLI dispatch, #489 Coder grant, #490 worker routes + KV store) plus the renderer + dispatch wiring (#491) and `ARTIFACTS` KV binding (`ae1eae1b`). Live in production. Web orchestrator can emit `create_artifact` and have it render inline; CLI path unchanged. Live-preview kind is intentionally stubbed pending sandbox-side dev-server orchestration (`create_live_preview` tool, token mint, proxy route). | PRs #487, #488, #489, #490, #491 + `ae1eae1b` |
| 2026-05-05 | Context-transformer pipeline shipped end-to-end: distillStage scaffolding (#478), `distillContext` → `DistillResult` shape alignment (#479), CLI migration to append-only transcript + per-hop `transformContextBeforeLLM` (#480), `cacheBreakpointIndex` threading for Anthropic-style prompt caching via OpenRouter (#481), and hybrid O(diff) persistence with `messages.jsonl` + slim `state.json` (#482). CLI message pipeline now routes through the same shared transformer as web; `state.messages` is canonical and append-only. | PRs #478, #479, #480, #481, #482 |
| 2026-04-26 | "UX: Preserving Context on Branch Creation" shipped end-to-end via slice 2 (#412 — conversation-preserving fork migration), slice 2.1 (#413 — "New Branch from Here" UI), slice 2.5 (#414 — `sandbox_switch_branch` + foreground branch-checkout/switch guard), and slice 3 (#415 — `sandbox_create_branch` in background coder jobs + four hardening fixes). Foreground/background result boundary locked in: foreground tools emit `branchSwitch` for UI routing (chat migration / selection); background tools emit `meta: { branchCreated?, branchSwitched? }` (defined in `lib/coder-agent-bindings.ts`) for observability only — no background result fires chat or routing side effects. `sandbox_create_branch` is wired for both surfaces; `sandbox_switch_branch` is foreground-only for now. | PRs #412, #413, #414, #415 |
| 2026-04-25 | PushStream Gateway Migration CLI tranche shipped in #402 + #403, sealing the migration end-to-end. `cli/provider.ts#streamCompletion` is now a thin retry/timeout wrapper around `openAISSEPump`; the two queue/notify shims that bridged its callback API into a `PushStream` for lib-side agent roles (`createDaemonProviderStream`, `buildPlannerPushStream`) are gone in favour of the native PushStream. Web and CLI share `lib/openai-sse-pump.ts` and `lib/reasoning-tokens.ts#normalizeReasoning` end-to-end. Behaviour shift: CLI's daemon and planner paths no longer inherit `streamCompletion`'s 3-attempt retry policy on 429/5xx/network — symmetric with the web side; `cli/engine.ts`'s REPL/headless path keeps `streamCompletion` retries unchanged. | `docs/archive/decisions/PushStream Gateway Migration.md` rows 19–20 |
| 2026-04-25 | PushStream Gateway Migration completed across PRs #365 → #401. Every agent role iterates `PushStream<M>` events directly; every provider routes through a native `<provider>Stream` consuming the shared `openAISSEPump`; both bridges, the `ProviderStreamFn` callback shape, `legacyChatPushStream`, `PROVIDER_STREAM_CONFIGS`, and the `streamSSEChat` / `streamSSEChatOnce` machinery are all deleted. CLI consumption (`cli/provider.ts`) tracked separately under "Selective CLI Adoption of Shared Runtime" — different surface, parallel work. | `docs/archive/decisions/PushStream Gateway Migration.md` rows 1–18 |
| 2026-04-21 | Verified and promoted two already-shipped items: the Sandbox Awareness Matrix is live through `[SESSION_CAPABILITIES]` / `[SANDBOX_ENVIRONMENT]` prompt blocks with TTL and lifecycle history, and the `pr` tool now returns inline review comments plus issue-thread conversation comments. | Documentation verification pass against current code/tests |
| 2026-04-14 | CLI Protocol/Schema Hardening shipped. `cli/protocol-schema.ts` is the canonical runtime validator for the pushd wire envelope and the nine delegation event payloads. Strict mode opt-in via `PUSH_PROTOCOL_STRICT=1`; daemon-integration test harness enables it so every handler test runs through the validator. Drift guard-rails re-extract delegation types and role literals from `lib/runtime-contract.ts` source at test time. Request/response envelopes and daemon-only event payloads (`session_started`, `approval_required`, etc.) are deliberately out of scope for this tranche. | `docs/archive/decisions/push-runtime-v2.md` Runtime schema validation section + `docs/archive/decisions/Web and CLI Runtime Contract.md` cli/protocol-schema.ts note |
| 2026-04-09 | Pivoted Sandbox Telemetry from external analytics to an internal Sandbox Awareness Matrix. Models will get TTL and lifecycle events directly in prompts instead of building operator dashboards. | Chat decision |
| 2026-04-05 | Shared runtime convergence tranche shipped: task-graph runtime, typed memory, delegation briefs, run-event vocabulary, and role-context semantics now live in shared `lib/`; further CLI work should be selective product adoption, not blanket parity extraction | Implementation session + `docs/archive/runbooks/Shared Runtime Convergence Plan.md` |
| 2026-04-05 | Task graph orchestration, graph memory, typed context-memory retrieval/invalidation/packing, and task-level trace events shipped as the new orchestration baseline | Implementation session + `docs/archive/runbooks/Task Graph Orchestration Plan.md` + `docs/archive/decisions/Context Memory and Retrieval Architecture.md` |
| 2026-04-05 | In-app repo creation should be treated as "publish workspace to GitHub" first; Phase 1 publish UI/workspace handoff shipped | Implementation session + `docs/runbooks/Workspace Publish to GitHub Plan.md` |
| 2026-02-22 | Roadmap focus shifted to CLI/TUI terminal UX improvements; harness reliability program moved from active checklist to completed baseline priority | Documentation/roadmap update |
| 2026-02-20 | Push CLI foundation and harness transition shipped in staged commits (`53b3c29`, `9b10856`, `aa80d7f`, `9ec31c5`) with checkpoint resume intentionally deferred for CLI | Implementation session |
| 2026-02-20 | Track D server-side background jobs deferred; resumable sessions remain the active interruption-recovery strategy | Product scope decision |
| 2026-02-20 | Push CLI direction changed: no full-screen TUI; target transcript-first interactive REPL with Claude Code/Codex muscle memory | Product scope decision |
| 2026-02-20 | Resumable Sessions hardening pass fixed resume race conditions and lock handling after merge review | Commit 61a262a |
| 2026-02-19 | Resumable Sessions completed (Phase 2-4): resume banner UX, sandbox reconciliation, multi-tab lock, checkpoint size controls, and resume telemetry | PR #106 (`3ded27f`) |
| 2026-02-19 | Resumable Sessions Phase 1 shipped: local checkpoint persistence and interrupted-run detection baseline | PR #105 (`d311af6`) |
| 2026-02-19 | Agent Experience Wishlist (Track C) fully implemented | Commit 0336f11 + follow-ups |
| 2026-02-19 | OpenRouter catalog updated: Sonnet 4.6, Gemini 3.1 Pro Preview added; Codex 5.3 removed | Commits f323e1b, 4b97df9 |
| 2026-02-09 | Root `ROADMAP.md` is canonical; `docs/` is draft space | Team decision in chat |
| 2026-02-09 | Scratch workspace vision (then called Sandbox Mode): real ephemeral workspace + explicit promotion paths | `docs/archive/Sandbox mode.md` |
| 2026-02-08 | Scratch workspace v1 descoped: no in-app repo creation (latency/sync concerns); zip download is the only export path; onboarding entry point is v1 priority | `docs/archive/Sandbox mode.md` revision |
| 2026-02-09 | Scratch workspace v1 implemented: two entry points, scratch-workspace system prompt, `sandbox_download` tool + card, expiry warning banner, persistent download button in header; export format is tar.gz (not zip) | Implementation session |
| 2026-02-12 | Workspace shell consolidated into one mobile hub trigger with `Files`, `Diff`, `Console`, and `Scratchpad` tabs | `docs/archive/Workspace Hub Sprint Plan.md` + implementation session |
| 2026-02-12 | Branch selection now surfaces existing branches across Home/workspace, with in-context delete from the workspace selector | Implementation session |
| 2026-02-12 | Home header simplified to compact account menu with Settings + guarded Disconnect and GitHub avatar | Implementation session |
| 2026-02-13 | Harness-first reliability promoted as canonical planning focus; browser tooling treated as one capability within harness work, not a standalone roadmap pillar | `docs/archive/runbooks/Harness Reliability Plan.md` |
| 2026-02-14 | Track B phase 1 shipped: `sandbox_read_file` supports line ranges with numbered tool output and out-of-bounds empty-range warnings | `docs/archive/runbooks/Harness Reliability Plan.md` + implementation session |
| 2026-02-17 | Activated Hashline protocol (`sandbox_edit_file`) in the harness to replace full-file rewrites with surgical edits | Implementation session |

## Promotion Checklist (Draft -> Canonical)

An item should be promoted from `docs/` to this roadmap only if all are true:

- problem statement is clear
- v1 scope is bounded
- success criteria are testable
- ownership is clear (person/agent/phase)
- non-goals are explicit

## Notes for AI Collaborators

- Always read `ROADMAP.md` first for current priorities.
- Treat `docs/` as exploratory unless explicitly referenced by a roadmap item.
- If implementation diverges from a draft, update this roadmap with the new decision.
